namespace DynaDocs.Sync.Notion.Dtos;

/// <summary>An empty <c>{}</c> config — the property-schema shape Notion expects for types that
/// carry no extra options on create (title, rich_text, number, date, single_property).</summary>
public sealed class NotionEmptyConfig { }
