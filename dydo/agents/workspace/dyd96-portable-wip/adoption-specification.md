## Spec

### Authority, outcome and status

DYD-96, Feature/AFK, on `codex/DYD-96-assurance-adoption` in `C:/Users/User/Desktop/Projects/DynaDocs/.worktrees/dyd96-adoption`, clean before specification at reviewed feature `37acc1f3706bf925e5230fb58c4a790dba769d62`. The sole captain owns delivery; the Specifier authors this amendment and no production. DR048 and completion plan `dydo/project/plans/dydo-3-completion.md` at `12112f507b4d1726310b96150f385a52bd1986f9` govern. Historical receipt, recursive assertion-body coverage, shadow-assembly and semantic mutation-selector requirements are superseded by this full Spec/Plan. Their commits and evidence remain retained.

The bounded C# mechanism respecification starts from preserved UNREVIEWED WIP `633a78e10dc7fcd5992fcb437e87219486ca534d` on the same branch and isolated root. It changes only the C# mechanism replacement section, 14 actions in the existing 101-path ownership table, and the portable native-evidence packet. It does not validate or modify the production WIP and requires a fresh independent SPEC review before implementation resumes.

Deliver credible static and coverage mechanisms for all maintained C#, Python and npm JavaScript, preserving the merged public facade. First obtain a complete measured failure inventory and proven mechanisms, then assign and close valid remediation, and finally require G=0 at the integrated candidate. Neither a gap report nor G=1/2 closes DYD-96. DYD-103 owns separate actual mutation. DYD-105 owns the npm entry rename and waits for DYD-118 metadata; DYD-130 is the existing final Merge, blocked by DYD-129, followed by DYD-131. No parent-Done cycle.

**Specification shape:** the assurance measurements use explicit gates; the additive assurance-adoption.feature fixes the newly approved observable gate-adapter completion behavior. No new public command is introduced; the prior testing-facade.feature remains unchanged. The specify hop commits only the new feature and pins this complete evidence contract. Independent SPEC review is mandatory because the changes establish measurement credibility and a cross-Issue input interface.

### Approved public facade integration seam

The merged facade currently maps every normally completed nonzero child to failed/resultExit1 (`run_row`, lines242–247), even when the child correctly reports missing measurement as2 or completed interruption cleanup as130. This conflicts with assurance's required public meanings. The proposed bounded amendment reserves 0/1/2/130 for **non-test gate adapter** completion: passed/failed/invalid/interrupted. Preserve raw childExit; other completed nonzero gate exits are failed1. A normally completed test command keeps its existing zero/pass versus nonzero/fail1 mapping. Facade-received interruption keeps the reviewed cleanup and130 behavior. Invalid2 continues independent rows; interrupted130 stops later rows. No schema, parser, stack selection, forwarding, placeholder or lifecycle redesign.

The admiral explicitly approved this lower-level protocol correction under completion-plan section3 on 2026-09-06; it introduces no new human decision and does not reopen the historical DYD-113 PASS. Vendor command wrappers must translate their native tool exits into the gate-adapter contract. An adapter returns130 only after its owned cleanup completes; a vendor exit130 alone is not such a guarantee. The additive feature contains four scenario outlines and23 expanded examples: static/coverage/mutation x native0/1/2/17/130; test x native2/130 unchanged; three mixed independent-row cases (1 then2 then0); and three cleanup-before130 cases preventing later dispatch. Both canonical and derived runners pass the same actual subprocess suite. The resulting contract also binds DYD-103's adapter and DYD-91's example. The additive @DYD-96 feature is DynaDocs.Tests/Features/assurance-adoption.feature; the existing testing-facade.feature and historical evidence remain unchanged. Its exact edit ownership is separately posted before authoring.

### Operation and artifact contract

Public commands remain exactly `test --stack NAME [-- literal args]`, `all [--stack csv]`, `gate static|coverage [--stack csv]`, `gate mutation --since BASE [--stack csv]`, `capabilities`, `--force-run`, `--help`. `all` means tests; `--force-run` means test/static/coverage, never mutation. No new public output/cache/config selector and no retained facade replacement.

Configure each existing dotnet/python/node static and coverage capability with current-python argv `DynaDocs.Tests/coverage/gate_adapter.py --stack <name> --gate <static|coverage>` and one required repository-relative artifact `DynaDocs.Tests/coverage/results/adapters/<name>-<gate>.json`. It is a project adapter, not another user-facing dispatcher. Mutation remains unavailable Pending DYD-103. Preserve test argv behavior; the Node test adapter is updated to include the actual full maintained Node suite, including `npm/test/*.test.cjs` and the gate test files, using a small exact-discovery script `node_tests.cjs` that forwards any requested literal native test arguments. No npm package version/bin edit.

Each adapter allocates a unique owned directory under `DynaDocs.Tests/coverage/results/assurance/`, collects fresh facts, writes immutable raw reports there, then atomically publishes its summary at the configured stable artifact path. An exclusive create of `<summary>.lock` protects only that publication/campaign slot: collision returns2 and preserves the other owner's lock/artifacts; remove only a lock this invocation successfully created. No stale-lock stealing, process registry or scheduler. A crashed leftover is a diagnostic requiring attributable cleanup. Static and coverage are independent slots. Reports are not reused across invocations. Each adapter always emits its own report when its owned output is usable, including measured failures and collection gaps; its report publication schema and all six stable paths are covered by fixture tests. The unique report directory contains inventory.json and raw command reports, and the summary inventory pointer includes that exact directory, not a mutable global inventory alias; no output failure becomes success.

Summary schema1: `schema`, `candidate` (commit, dirty, sourceFingerprint), `stack`, `gate`, `inventory` (relative artifact path and sha256), `tools`, ordered `commands`, `collectors`, `findings`, `gaps`, `measurementComplete`, `exitCode`. Each command records exact argv/cwd, relevant supplied environment, raw native exit, elapsed time, stdout/stderr relative paths and hashes. A collector records name/status and raw artifact paths/hashes. Findings record gate, exact path and member/span when applicable, actual/threshold or witness, and raw reference. A gap records collector/path/reason and raw reference. Paths in reports are relative to the owning repository or report directory as explicitly named; no ambiguous suffix match. Independent collectors continue after a peer failure. Missing/unsupported/malformed/stale/ambiguous measurement is2, valid policy violation1, genuine success0, interruption130 after owned cleanup;2 outranks1 and130 outranks both. Child tool failure without accounted diagnostics is2. Source/config/tool inputs changing during collection invalidate the run with2.

### Inventory, roles and the mutation handoff

Retain `inventory.py` canonical case-sensitive repository-relative paths and byte hashes, but include tracked deletions explicitly instead of silently filtering them away. Enumerate tracked plus nonignored untracked files using NUL-safe Git operations. Union that with evaluated C# Compile items, package bin/main/scripts and explicit supported source extensions/shebangs. Report every maintained source and every structural exclusion. Missing/evaluated-outside/ambiguous sources are gaps. Generated `obj/bin`, dependencies and fixture data are excluded by reproducible origin, never names such as Generated in an authored class. The retired workflow's special path/hash exception is removed because that workflow is absent at the reviewed base.

`inventory.json` schema1 is the exact handoff to DYD-103: `schema`, `candidate` {`commit`,`dirty`,`sourceFingerprint`}, `files` sorted by exact canonical `path`, `sources` sorted by path, `excluded`, `projects`, `errors`. `files` rows are `{path,sha256}` for all existing maintained/config/test inputs and explicitly absent tracked deletions `{path,sha256:null,deleted:true}`. `sourceFingerprint` is SHA256 of UTF-8 compact JSON of those ordered rows with sorted object keys, Unicode escaped, with no trailing newline. `sources` rows are `{path,sha256,language,role,projects,executable,testFiles}`: language `cs|python|javascript`; role `target|test`; projects exact csproj paths (empty elsewhere); executable boolean; testFiles sorted exact paths. `excluded` records path, reason, and positive origin evidence, including a canonical-source digest for derived copies. `projects` records path, evaluated Compile paths, test-project boolean and assembly target path relative to root. An incomplete inventory has nonempty errors and cannot serve mutation; unknown properties/schema/duplicates are rejected. Include source and configuration input identity, not output artifacts or dependency installations. Consumers rehash current files and reject stale/mismatched inventory; they cannot convert errors to an empty campaign.

DYD-103 may use the exact target source set and widen changed selection to file/project/stack. This handoff promises no per-test case IDs, body witnesses, import-equivalence graph, changed member selector or selected-run receipts. DYD-103's independent Specifier must acknowledge the interface and nominate the exact artifact before production; DYD-96 Done is not its planning dependency.

Use `test-associations.json` schema1 `{schema:1,modules:[{module:"exact/source",tests:["exact/test"]}]}`. Every non-trivial executable target has at least one existing maintained test file. No wildcards, same-stem inference, empty/duplicate edges or skip fields. C# test roles come from evaluated test-project Compile membership plus explicit source content roles, not `DynaDocs.Tests/**`; gate metrics project remains target. Python and Node test files must be among the actual ordinary suite's discovered files and contain a native unittest/TestCase or Node test declaration; validate those suites run as part of the tests/coverage gate, without case-ID edge matching. C# support/fixture source in the test project is test-role evidence; a separate executable helper project remains target. Missing association is1; unknown/stale/invalid path or manifest is2. An association asserts intent only; it neither proves coverage nor excuses a floor. Module non-triviality is not a size waiver: any maintained executable target body qualifies; purely declarative modules remain inventoried with no executable denominator.

### Exact policy and five missing mechanisms

Static collection measures all maintained source, including test assertions/support files, with compiler warnings as errors/strictness, dead code, cognitive<=20, nonconstructor parameters<=7, supported nested ternary, dependency cycles and clone threshold requiring BOTH at least15 lines AND100 tokens. Clone collection compares the complete maintained source set across stacks; a selected static row reports witnesses touching that selected stack, preserving cross-stack witnesses. No tiers, classic CRAP, independent CC/nesting cap, suppression or per-file waiver.

Coverage runs ordinary **full suites**, and applies per-target-source module line>=80% and branch>=60%, and per-callable HCRAP `CC² * (1 - covered/total)³ + cognitive <=20`. Test assertion/fixture bodies supply evidence; they do not require recursive coverage of themselves. All maintained tooling, controllers, producers, entry points and language adapters are targets. Use real full-suite native hits and complete zero-hit source/method inventories; never infer function execution from a covered definition line, borrow another same-line callable's hits, use maximum percentages, or drop an unjoined method. Union exact hit/branch identities only after matching source and report identities. Zero branch denominator is N/A only when complete native/source facts prove branchless behavior; zero executable denominator requires positive declarative accounting. Generated async/iterator implementations backed by maintained source are measured, not excluded by CompilerGeneratedAttribute.

1. **Effective analyzer options:** adopt the retained Sonar package pin through `Directory.Build.props` (PrivateAssets all), selected Sonar S3776/S3358 and IDE0051/52/60 error severities, and threshold20/propertyThreshold20 in SonarLint.xml. Do not copy blanket `dotnet_analyzer_diagnostic.severity=none`: switch off only unrelated Sonar diagnostics without disabling unrelated compiler/analyzer diagnostics. Read effective evaluated options for product/test/metrics projects and verify actual native failing/passing fixtures for every configured rule. Keep warnings-as-errors active. The measurement producer may be built once with RunAnalyzers=false solely to obtain facts about broken code; the independent actual build/analyzer gate still runs fully and records failures.
2. **C# full coverage:** the complete replacement under **C# mechanism replacement — AltCover 9.0.102 eager** below supersedes only the Coverlet collection, report, complexity and join mechanics in the accepted contract. The isolation, full-suite, maintained-source, physical-method, generated-body, exact-identity and fail-closed policy obligations remain binding.
3. **Python full coverage:** use pinned coverage.py7.16.0 branch collection over the full unittest suite with subprocess collection enabled and explicit maintained target source paths, including never-imported sources. Combine native data only from the invocation-owned output. Native file/branch facts give module floors. Retain only `python_runtime.py`'s exact compiled-code/AST CallableWitness mechanism for callable line counters; it disambiguates repeated names, lambdas and definition lines. A new small `python_coverage.py` starts one collector/witness per ordinary process and exports a flat per-process counter file plus native coverage data at normal completion. A generated invocation-local `sitecustomize.py` provides that same startup to Python subprocesses via inherited PYTHONPATH and one configuration path; no nested logical sessions, broker, view union, adoption or parent receipt graph. Duplicate startup in the same process must not replace active collection. Combine exact same-source counters across ordinary processes. Explicitly exercised child-only callable fixtures must appear in coverage; startup/report failure is2, never a claimed green row. Unit tests of the counter/adapter are measured as ordinary target calls; they do not recursively launch the repository coverage gate to prove themselves. Plain file/region coverage is insufficient for ambiguous callables: installed coverage.py jsonreport.py keys regions by name and can overwrite repeated names. Preserve fail-closed native identity checks.
4. **JavaScript full coverage:** use ordinary c8 12.0.0 with native V8 subprocess coverage for every real Node test file, with `--all`, explicit maintained target include paths, exclusion defaults disabled as needed, JSON and LCOV output in the owned directory. Keep `js_metrics.cjs` official SonarJS/ESLint facts and exact UTF-16 body spans. A small new `javascript_coverage.cjs` joins c8/Istanbul fnMap/statement/branch maps plus native V8 function ranges to those exact source spans, counts a never-called function as uncovered, and rejects missing/ambiguous source/body joins. Module entry/top-level executable statements stay in the floor; callbacks are separate cognitive scores. No retained JS loader/runtime/session/receipt/extra-entry instrumentation. Fixtures must prove CommonJS, ESM, child-only CLI, never-imported file, empty callable, two same-line callbacks and source-map/path ambiguity. The known extensionless npm bin remains an explicit measurement gap until DYD-105 merges; do not rename it here or synthesize a filename for analyzers.
5. **Associations:** replace selected-case witness validation with the simple file manifest above. Keep full-suite execution and coverage as independent evidence. Test helpers/assertions are not targets solely because they are maintained; metrics and gate executables are targets even under a test directory. Validate new/untracked/renamed/deleted modules and test files, many-to-many intent, exact-case collisions, and missing relations.

### C# mechanism replacement — AltCover 9.0.102 eager

This section is the complete replacement for accepted C# mechanism 2 and for every later Coverlet-specific command, report-format, complexity or join reference. All clauses not expressly replaced here remain unchanged. The immutable native basis is `native-altcover-evidence/SHA256SUMS`; the original Coverlet failure remains preserved by the WIP packet and is not reclassified. AltCover's default buffered collection failure and its eager success are both preserved in that packet.

**Tool and exact campaign.** `.config/dotnet-tools.json` replaces `coverlet.console` 6.0.4 with the local-development tool `altcover.global` 9.0.102 and command `altcover`; `dotnet tool restore --tool-manifest .config/dotnet-tools.json` is the only restore command. Remove `coverlet.collector` 6.0.4 from `DynaDocs.Tests/DynaDocs.Tests.csproj` and delete `DynaDocs.Tests/coverage/coverage.runsettings`; no MSBuild collector or runsettings behavior remains. `run_tests.py --assurance-output <new-absolute-owned-directory>` still creates one invocation-owned isolated snapshot, copies the exact dirty/deleted/renamed candidate into it, runs the one finite Windows Job campaign, publishes its evidence outside the snapshot, verifies owned cleanup and then removes only that registered snapshot. A coverage proof supplies no selected test arguments: any `--filter`, project narrowing or selected-test suffix is invalid exit 2 for this gate. The ordinary complete suite remains this single command inside the isolated root:

```text
dotnet test DynaDocs.sln -c Debug --no-build -p:RunAnalyzers=false -p:UseSharedCompilation=false -- RunConfiguration.TreatNoTestsAsError=true
```

First build `DynaDocs.sln` and `DynaDocs.Tests/coverage/metrics/GateMetrics.csproj` in Debug with the accepted no-analyzer measurement-only flags. Stage byte-for-byte originals for `bin/Debug/net10.0/dydo.dll`, `DynaDocs.Tests/bin/Debug/net10.0/DynaDocs.Tests.dll`, and `DynaDocs.Tests/coverage/metrics/bin/Debug/net10.0/GateMetrics.dll`, with their portable PDBs, under the invocation's raw output. Then run exactly one prepare command over those three build directories (repeated `--inputDirectory`), in place only because the entire root is invocation-owned:

```text
dotnet tool run altcover -- --inputDirectory=<isolated>/bin/Debug/net10.0 --inputDirectory=<isolated>/DynaDocs.Tests/bin/Debug/net10.0 --inputDirectory=<isolated>/DynaDocs.Tests/coverage/metrics/bin/Debug/net10.0 --inplace --report=<owned-raw>/csharp/template.opencover.xml --reportFormat=OpenCover --eager --localSource --visibleBranches --assemblyFilter=^(?!(dydo|DynaDocs.Tests|GateMetrics)$).*
```

The negative-lookahead exclusion is pinned fixture behavior: only exact assembly names `dydo`, `DynaDocs.Tests`, and `GateMetrics` are instrumented; dependencies remain executable but uninstrumented. The command carries no `--showGenerated`. AltCover 9.0.102 documents that flag as `Mark generated code with a visit count of -2 (Automatic) for the Visualizer if unvisited`, and the 2026-09-09 probe (`showgenerated-marker-evidence.md`) shows what it does: at prepare time every sequence and branch point of every attribute-generated (`GeneratedCodeAttribute` on the method or its type) or compiler-generated body (`CompilerGeneratedAttribute`, `<>c` lambdas, async and iterator `MoveNext`) is written as `vc="-2"` in the template, whose `MethodPoint` rows carry no marker; after collection the executed points carry their counts while every unvisited sequence, branch and method point carries `-2`, untaken branches inside executed `MoveNext` bodies included, so a never-executed generated method reports `visited="true"` and 100% in the vendor summaries. Without the flag the same 27 methods, the same `File` row and byte-identical point and branch identities are present with `vc="0"` and `visited="false"`, and no `skippedDueTo` appears either way; the flag changes nothing but the marker and the fields derived from it. `vc` is therefore a non-negative count in every consumed row; a negative `vc` remains exit 2 (`Invalid native sequence value`, branch ownership mismatch) and the marker is never decoded. Run the complete test command through:

```text
dotnet tool run altcover -- runner --recorderDirectory=<isolated>/DynaDocs.Tests/bin/Debug/net10.0 --workingDirectory=<isolated> --executable=dotnet --outputFile=<owned-raw>/csharp/coverage.opencover.xml --summary=N -- test DynaDocs.sln -c Debug --no-build -p:RunAnalyzers=false -p:UseSharedCompilation=false -- RunConfiguration.TreatNoTestsAsError=true
```

The full suite itself exercises the instrumented root `dydo.dll` through the existing out-of-process CLI fixtures and the metrics executable through real source/project/assembly fixture calls; it must not rebuild either target. A post-run timestamp/hash check makes any rebuild or byte replacement exit 2. No separate selected producer shortcut contributes acceptance coverage. The runner's raw exit, argv, cwd, supplied environment, elapsed time, stdout and stderr are recorded and hashed. The only native coverage format consumed is AltCover OpenCover XML. JSON/Cobertura location, merge and cross-format-equality obligations are deleted.

**Identity before and after instrumentation.** Before prepare, emit `csharp/identity-pre.json`. For every collected assembly path it contains original DLL and portable-PDB SHA-256 and byte count, MVID, exact assembly name and root-relative path; every MethodDef with `HasBody` contains the integer metadata token and Cecil physical full signature; every non-hidden sequence point contains IL offset, the classified portable-PDB document identity and its origin under the document classification below, start/end line and zero-based column, document checksum algorithm and checksum; every collected assembly contains its document table, the verbatim portable-PDB document URL of every document reached by a non-hidden sequence point mapped to that classified identity; every referenced maintained source contains SHA-256. Duplicate paths, names, MVIDs, tokens, signatures, PDB documents or source identities are invalid except for the exact alias rule below.

**Document classification.** The assembly producer runs as `dotnet GateMetrics.dll --assembly <dll> --root <root> --project <csproj>`; the owning project supplies the locked `obj/project.assets.json` exactly as `ProjectMetrics.SourceIdentity` already resolves compilation documents, and `csharp_coverage.py` passes `DynaDocs.csproj`, `DynaDocs.Tests/DynaDocs.Tests.csproj` and `DynaDocs.Tests/coverage/metrics/GateMetrics.csproj` for `dydo`, `DynaDocs.Tests` and `GateMetrics`; the old three-argument grammar `--assembly <dll> --root <root>` is a grammar rejection, never a classification: the `Program.cs` `args switch` replaces its `["--assembly", var assembly, "--root", var root]` arm with the four-argument arm, so that vector falls to the `_` arm, whose `ArgumentException` message becomes exactly `Expected --syntax, --project <csproj> --root <root> or --assembly <dll> --root <root> --project <csproj>.`; the existing catch writes that one line to stderr and returns 2 with empty stdout, and no assembly is opened, so no document URL is printed (at HEAD `Program.cs:12` still accepts that vector and exits 0 with the assembly facts on stdout). Every document reached by a non-hidden sequence point of a required assembly is classified into exactly one origin by reproducible origin, never by name, suffix or on-disk existence: `maintained`, a document whose absolute URL is under the inventory root and has no `obj` path segment, identity its root-relative path; `generated`, a document under the inventory root with an `obj` path segment (MSBuild intermediate output, e.g. Reqnroll code-behind `DynaDocs.Tests/obj/Debug/net10.0/Features/<name>.feature.cs`, `DynaDocs.Tests/obj/Debug/net10.0/xUnit.AssemblyHooks.DynaDocs_Tests.cs` and source-generator trees such as `obj/Debug/net10.0/generated/System.Text.Json.SourceGeneration/.../DydoConfigJsonContext.g.cs`), identity its root-relative path; `package`, a document outside the root whose URL lies under a `packageFolders` entry of the owning project's locked assets and whose first two path segments name a `libraries` entry, identity `nuget:<package>/<version>/<path>` exactly as the compilation level emits it (`nuget:microsoft.net.test.sdk/18.0.1/build/net8.0/Microsoft.NET.Test.Sdk.Program.cs`). Any other document is the failing control: a URL that is not an absolute path, a document outside the root with no locked assets file, one under a package folder whose `<package>/<version>` is not in `libraries`, or one anywhere else is exit 2 with empty stdout and a message ending `outside inventory root: <exact URL>`; nothing is skipped, projected or forgiven. The classifier is the one `ProjectMetrics.SourceIdentity` function exposed to `AssemblyMetrics` behind an absolute-URL check, so both levels produce byte-identical identities. Every `SourcePoint` carries `path` (the identity) and `origin` (`maintained`, `generated` or `package`) beside its existing offset, span, `checksum_algorithm` and `checksum`; `AssemblyFacts` carries `documents`, an ordinally sorted map from verbatim PDB URL to identity; `CompiledMethod.generated` (the `CompilerGeneratedAttribute` flag) is unrelated to document origin and unchanged. The checksum fields of a generated or package document are the PDB's own values, recorded as identity and compared across `identity-pre`, `identity-instrumented` and `identity-post`; no generated or package bytes are read from disk or from embedded source, because those documents never enter a denominator, and their on-disk existence is irrelevant both to classification and to the campaign (AltCover 9.0.102 `--localSource` still instruments and reports every method of an assembly that has at least one local document, as the evidence shows for a source generator without `EmitCompilerGeneratedFiles`). Maintained documents keep the existing on-disk checksum verification in the join. The measured facts for the three required assemblies at this hop are pinned in `pdb-document-classification-evidence.md`: `dydo` 76 maintained and 29 generated documents, 552 generated-only methods all under `obj/Debug/net10.0/generated/`; `DynaDocs.Tests` 101 maintained, 7 generated and 1 package document, `AutoGeneratedProgram::Main` (token 100663421) being the only package-only method, while the six `.feature` documents reached through Reqnroll `#line` pragmas are maintained by origin and never joined because the test assembly is collected for identity only; `GateMetrics` 19 maintained documents; no method in any assembly mixes origins. Implement in this order, each step red before green under the focused cases named below: the `SourcePoint` and `AssemblyFacts` fields; `ProjectMetrics.SourceIdentity` visibility; `AssemblyMetrics.Collect(assembly, root, project)` with the failing control (a missing project file is `Exact owning project is required.`); the `Program.cs` grammar; the `csharp_coverage.py` assembly-to-project map, `--project` argument and document-table comparison; the `csharp_join.py` resolution and accounting; then `coverage-tools.md`.

Byte-identical assembly copies are one module equivalence class keyed by `(exact assembly name, original DLL SHA-256, original PDB SHA-256, MVID, SHA-256 of the ordered MethodDef-token/full-signature/non-hidden-PDB-point rows)`. Every copy contributes its normalized isolated-root-relative path to a sorted alias list; the ordinally smallest path is the canonical representative. Same name or MVID with any different key field is exit 2. AltCover must emit exactly one `Module` row for the class. Its normalized `ModulePath` must equal one recorded alias, and its `hash` after removing hyphens and normalizing hex case must equal SHA-1 of the staged original DLL, as pinned by the native packet; the consumer then replaces that presentation path with the canonical representative. An unknown alias, hash mismatch, two report modules for one class, or one report module matching two classes is exit 2. Focused passing fixtures present two byte-identical paths and let `ModulePath` select each alias in turn, producing the same canonical join. Failing fixtures cover differing DLL/PDB/method-map bytes, same-MVID conflict, unknown `ModulePath`, module-hash mismatch, duplicate report rows and ambiguous class matches. This permits the ordinary root/test-output copies without suffix or path guessing.

Prepare may change only the invocation-owned working DLLs, may renumber their MethodDefs and shift their rewritten IL instruction offsets, and may add recorder companions. Immediately after prepare, emit `csharp/identity-instrumented.json`: instrumented DLL/PDB hashes and byte counts, MVID, MethodDef token/full-signature inventory, instrumented Cecil method-body IL offsets and portable-PDB document map, including point identities/origins and the URL-to-identity document table.

The whole original-versus-instrumented distinction is fixed here. Canonical module identity is the staged original's exact assembly name, root-relative alias, DLL SHA/byte count, portable-PDB SHA/byte count and MVID. Canonical method identity is the original assembly's MethodDef token and Cecil physical full signature, checked by the prepare template's `(MetadataToken, Name)`. Canonical sequence and branch identities use the original portable-PDB and prepare template: original method token; original PDB sequence-point IL offset; document URL and classified path/origin; document checksum algorithm/checksum; source start/end line and zero-based column; and the template's native sequence/branch offsets, offsetend, path, ordinal, `uspid`, `fileid` and `sl` where that row supplies them. The collected report may change visit counts only; all structural keys must remain the template keys. By contrast, the instrumented DLL SHA/byte count, instrumented Cecil MethodDef token/order and instrumented Cecil method-body IL offsets are diagnostics: they may change and never supply or replace a module, method, sequence or branch join key. The staged originals and maintained source must still match `identity-pre.json`; the instrumented MVID, exact portable-PDB bytes/SHA/byte count, document table, point count and every PDB/source `(document URL, classified path, origin, checksum algorithm, checksum, span)` tuple must remain unchanged. Missing, duplicate or ambiguous original/template identity, use of an instrumented diagnostic as a join key, or mismatch in any required stable fact is exit 2.

The prepare-time `template.opencover.xml` and its module binding are against the staged original assemblies. A template module joins only through the original assembly's complete equivalence key and canonical representative after the exact `ModulePath` alias and original-DLL hash validation above. Within that bound module, every template method's `(MetadataToken, Name)` must resolve exactly once to the original `HasBody` MethodDef at that integer token, and the rendered `Name` must equal that original method's Cecil physical full signature after the one documented XML encoding normalization. The name is a check, never a lookup. Every template method must satisfy that mapping; a missing original token, duplicate template token, duplicate original resolution or name mismatch is exit 2. The collected report must preserve exactly that template method namespace and may join only through the validated template-to-original mapping; instrumented MethodDef tokens are never join candidates. No suffix, short signature, source line/span, class name, token-only-across-modules or instrumented-byte join is allowed.

After collection and before snapshot cleanup, rehash source, staged originals, working assemblies/PDBs, tool manifest/lock and the AltCover executable; mutation during collection is exit 2. Restore each working DLL and portable PDB byte-for-byte from its staged original, then emit `csharp/identity-post.json`. Pre/post comparison must prove exact DLL/PDB SHA-256 and byte counts, MVID, portable-PDB points/documents and maintained-source SHA-256; it also records the exact original module and template-method relations. Missing restoration, duplicate restored artifact or any pre/post mismatch is exit 2. Only after this proof may the registered invocation-owned snapshot be removed.

**Coverage accounting.** Preserve every physical `MethodDef` with maintained non-hidden PDB points, including zero-hit methods and generated async, iterator, local-function and lambda bodies, whose zero hits are native `vc="0"` rows and never a decoded marker. The join settles every physical method of a joined target by the origins of its points: a method with no non-hidden point is inventoried in `accounting` with reason `no non-hidden portable-PDB points`; a method whose points are all `generated` or `package` is inventoried with reason `excluded by origin`, its sorted `origins` and sorted `documents`, contributes to no line or branch denominator (DR 048 path exclusion by reproducible origin) and is never dropped silently; a method with only `maintained` points is measured. Those documents must agree with the compilation level of the same target: every `maintained` identity must be a `files` path of the project facts and every `generated` or `package` identity must be in its `generated_files`, otherwise exit 2 naming the identity (`Maintained PDB document absent from source inventory`, `Generated PDB document absent from compilation inventory`). A method mixing `maintained` with `generated` or `package` points is exit 2 naming its identity and documents; no such method exists in the three required assemblies, and admitting one needs a bounded amendment with a real fixture, not a partial measurement. `SourceBehavior`, semantic symbol keys, state-machine kickoff metadata and PDB points assign each measured physical method to exactly one maintained source callable; nested source callables own their own physical bodies and are excluded from the enclosing callable. Missing, duplicate or multiple ownership is exit 2. For a source callable, line coverage is the union of its owned physical method sequence rows keyed by `(document checksum/path, source line)`; a line is covered when any row for that same callable has `vc > 0`. Module line coverage unions the same rows by module source line. Same-line rows from different physical tokens never lend hits.

Each branch identity uses only fields the native row supplies: `(module equivalence key/canonical representative, parent MethodDef token, uspid, fileid, sl, offset, offsetend, path, ordinal)`. `fileid` must resolve through that report module's exact `File uid/fullPath` table, whose every `fullPath` must equal verbatim one URL of the recorded document table (AltCover writes the PDB document URL unchanged, including the absolute package or `obj` path of a generated document), to the classified document identity: for a maintained document the already verified PDB document path/checksum and source owner, for a generated or package document the same identity its PDB points carry, never a foreign file; `sl` must be a valid source line owned by that physical method. `vc` is the non-negative visit count, not identity. No column or end line is synthesized, and no exact PDB span join is claimed. Missing/duplicate/non-integer native fields, duplicate `uspid`, an unresolved or foreign `fileid`, a `File` row whose `fullPath` is absent from the document table (`Report file outside PDB document inventory`), a line outside the method's verified PDB/source ownership, or two rows with the same complete identity are exit 2. Module branch coverage is computed only as `count(descendant BranchPoint rows with integer vc > 0) / count(all actual descendant BranchPoint rows)`. Root, module and method `Summary` branch totals, percentages and any converted projection are ignored and must not be copied into normalized facts. A zero branch denominator is N/A only after the complete physical/source inventory proves the module has no branch rows. Focused passing fixtures preserve two branches on one source line and the child fixture's same-line lambdas by parent token and native row identity; failing fixtures collide ordinals/offsets across tokens, duplicate one full row, point `fileid` at the wrong source, omit a native field and place `sl` outside its method owner.

**Policy cyclomatic complexity.** DR048 fixes source-callable HCRAP, says CC counts every `case`, and keeps classic source-path CC only inside the coverage term. The previous accepted mechanism incorrectly delegated that value to Coverlet's collapsed physical rows. AltCover exposes a second physical convention: its `if` lambda reports CC2 but its one-ternary lambda reports CC3. Neither vendor value is policy input.

Use a static Roslyn 5.9 syntax producer in `SourceMetrics`, calibrated to the already pinned `SonarAnalyzer.CSharp.Metrics.CSharpCyclomaticComplexityMetric` convention. The portable calibration witness proves the official metric gives straight1, if2, ternary2, `if (a && b)`3, try/finally1, any number of plain or filtered catches1, two-case-plus-default switch expression4, `||`2, `??`2, `?.` followed by `??`3, `??=`2, four loops5, logical `and`/`or` pattern3, and three-arm guarded switch expression4. It also proves raw Roslyn CFG `E-N+2P` is not the policy convention: it reports try/finally2, switch3 and misses pattern decisions. Therefore no CFG edge normalization is permitted.

For each exact source callable already emitted by `SourceMetrics`, policy CC is `1 +` the count of these descendant Roslyn syntax nodes inside that callable's body: `if`; conditional expression `?:`; `for`; `foreach` and `await foreach`; `while`; `do`; every switch-statement `case` and `default` label; every switch-expression arm including discard/default; binary `&&`, `||`, and `??`; assignment `??=`; conditional access `?.`/`?[]`; and binary patterns `and` and `or`. `else`, `catch`, catch filters, `finally`, `try`, `throw`, `goto`, `await`, `yield`, `using` and `lock` add zero by themselves. A guard contributes only the listed decisions inside its expression. When traversal reaches a nested lambda, anonymous method or local function, it stops; that node receives its own baseline and descendant count. The same stop applies across accessors and other separately inventoried source callables. This reproduces the established Sonar convention without source rewriting, shadow compilation or physical-report inference, while making nested callable ownership exact.

Pinned fixtures must compare this producer with the official Sonar metric on callables without nested callable bodies, then separately assert nested ownership: straight and empty bodies; `if`/`else if`; `?:`; `&&`, `||`, `??`, `??=` and conditional access; `for`, `foreach`/`await foreach`, `while`, `do`; switch statements/expressions including default/discard, multiple labels, guards and relational/logical/list/property patterns; `try/finally`, one and multiple catches and filters; local functions, lambdas, accessors, constructors/initializers, async, iterators and top-level statements. The binding values above and the physical fixture's if2/child ternary2 are exact. Any C# decision syntax accepted by the pinned compiler but absent from this exhaustive list, any disagreement with the pinned Sonar metric outside the intentional nested-callable isolation, or a callable missing one exact source owner is evidence-incomplete exit 2 and returns for bounded respecification. AltCover `cyclomaticComplexity` remains raw vendor evidence only. This metric feeds HCRAP and creates no independent CC gate.

**Required attacks and failures.** Native fixtures prove: an executed and a never-executed physical method; overlapping async source and generated state-machine bodies; one-line/same-span callables; two same-line conditional lambdas whose branch ordinals collide; and a parent launching a child whose sole relevant hit is the child's lambda. The last fixture must reproduce default buffered failure from the immutable packet and then, under the production `--eager` prepare command, retain `11 + 13 = 24` visits, the executed child token/sequence/one-of-two branches, and the never-invoked token at zero. Full-suite acceptance additionally requires these fixtures through `run_tests.py`; direct fixture execution alone is not proof.

The falsifiable original-token and restoration evidence is preserved under `DynaDocs.Tests/coverage/results/native-g-20260909-hop2-01`, `DynaDocs.Tests/coverage/results/native-g-20260909-hop2-02`, `DynaDocs.Tests/coverage/results/native-g-20260909-hop2-03`, `DynaDocs.Tests/coverage/results/native-g-20260909-hop2-journal.md` and the retained hop3-02 data. `DynaDocs.Tests` has 3,851 pre/post original MethodDefs, 124 token-stable and 3,727 token-changed instrumented MethodDefs, 3,157 template methods, 3,157 template-to-original matches and only 64 template-to-instrumented token/name matches; 12,012 of 12,050 comparable instrumented Cecil IL offsets changed. `GateMetrics` has 493/493 pre/post, 39 stable, 454 changed, 273 template, 273 original matches and 32 instrumented matches; 519 of 689 comparable instrumented Cecil IL offsets changed. `dydo` has 1,974/1,974 pre/post original MethodDefs; its 1,927 shared token keys comprise 64 same-identity and 1,863 different-identity pairs, with 47 pre-only and 47 post-only token keys; the identity/key/kickoff multiset has 0 removed and 0 added, so 1,910 of 1,974 original methods did not preserve token association. It has 1,610 template methods, 1,610 template-to-original matches and only 64 template-to-instrumented token/name matches; 12,015 of 12,237 comparable instrumented Cecil IL offsets changed. In all three assemblies the exact portable-PDB bytes/SHA, document table, point counts and source path/checksum/span/origin facts remained stable. These are pinned observations proving the binding rule, not policy thresholds.

Missing, duplicate, stale or ambiguous module/token/PDB/source/sequence/branch/source-CC facts; source/config/tool/original/instrumented-assembly mutation; a prepare or runner nonzero exit; missing/template-only/malformed output; timeout; incomplete child data; or publication collision is evidence-incomplete exit 2. A complete measured policy violation is exit 1. Interruption is 130 only after the Windows Job is empty, all owned descendants/handles are accounted for, raw evidence is copied, and owned cleanup succeeds. Cleanup failure is 2, including after interruption. The stable summary lock remains exclusive-create and atomic-publication as accepted; never steal a lock or overwrite another invocation. Before any cleanup, copy the template and collected XML, all residual `.acv` files (including partial/failed runs), pre/instrumented/post identities, original DLL/PDB copies, source hashes, manifest/tool/executable hashes, and every command stdout/stderr/exit into the external invocation-owned evidence directory. Preserve a failed attempt beside the later success; never replace it.

**Focused proof and retained joins.** Replace Coverlet-specific cases in `test_csharp_coverage.py` and `test_csharp_join.py` with AltCover prepare/runner, descendant native branch-row, exact module alias/token, duplicate/stale identity, raw-failure preservation and eager child-process cases. Expand `test_csharp_metrics.py` for the Sonar-calibrated Roslyn syntax-node convention, nested-callable isolation and all constructs above. Retain the AssemblyMetrics, SourceBehavior, MethodIdentity, portable-PDB checksum, source-callable, state-machine kickoff, constructor-fragment and zero-hit accounting tests after adapting their coverage input to tokened OpenCover. Add a prepared-assembly fixture whose MethodDefs are renumbered and Cecil IL offsets shift while every template `(MetadataToken, Name)` and sequence/branch structural key still resolves exactly once against the original/PDB/template; remove any assertion that instrumented tokens or Cecil offsets equal originals, and make missing, duplicate, ambiguous or wrong-name original/template mappings fail. Add restoration attacks that change one DLL hash, PDB hash, MVID, PDB document identity and maintained-source hash in turn, and require each pre/post comparison to fail. Retain `test_runner.py`, `test_orchestration.py` and `test_windows_job.py` isolation, publication, timeout, interruption and cleanup attacks. No custom coverage engine, source rewriting, shadow compilation or shadow assembly is permitted. The document classification adds these focused cases, fixture source as data strings: in `test_csharp_metrics.py`, `compiled_facts` and the portable-PDB test pass `--project` and assert origin `maintained` on every point and a one-entry document table for `Source.cs`; `test_sdk_generated_pdb_document_is_classified_by_locked_nuget_origin` builds a temporary project referencing `Microsoft.NET.Test.Sdk` 18.0.1 and asserts that the points of `System.Void AutoGeneratedProgram::Main(System.String[])` carry origin `package` and identity `nuget:microsoft.net.test.sdk/18.0.1/build/net8.0/Microsoft.NET.Test.Sdk.Program.cs`, that the document table maps the absolute package URL to it and that `--project` lists the same identity in `generated_files`; `test_in_root_obj_compile_item_is_classified_generated` compiles an explicit `<Compile Include="obj/Gen.cs" />` item and asserts origin `generated` and identity `obj/Gen.cs` at both levels; `test_source_generator_documents_are_classified_generated_at_both_levels` uses `EmitCompilerGeneratedFiles` and a `JsonSerializerContext` and asserts that every generated-only method's points are origin `generated` under `obj/Debug/net10.0/generated/` and a subset of `generated_files`; `test_unaccounted_pdb_document_fails_with_its_url` reuses the SDK fixture in three legs: the two document-classification legs, `obj/project.assets.json` deleted and `--project` naming a restored project that does not lock the SDK, each assert exit 2, empty stdout and the absolute package URL after `outside inventory root` on stderr; the third leg, `--project` omitted (`--assembly <dll> --root <root>`), is the grammar rejection of the Document classification paragraph and asserts exit 2, empty stdout and stderr whose stripped content equals exactly `Expected --syntax, --project <csproj> --root <root> or --assembly <dll> --root <root> --project <csproj>.`, no URL, red at HEAD where that vector exits 0. In `test_csharp_join.py`, the fixture facts gain `origin` and `documents`; `test_report_file_rows_resolve_generated_documents_to_classified_identities` places absolute package and `obj` paths in the `File` table and asserts that their rows join under the `nuget:` and `obj/` identities and that a `fullPath` absent from the table fails with `outside PDB document inventory`; `test_join_accounts_generated_only_methods_by_origin` drives `join_methods` with one maintained, one generated-only, one package-only and one point-free method and asserts the two accounting reasons, `origins`/`documents`, and denominators from the maintained file alone; `test_join_fails_closed_on_document_inventory_inconsistency` asserts errors for a generated identity missing from `generated_files`, a maintained identity missing from `files`, and a mixed-origin method. In `test_csharp_coverage.py`, `test_identity_comparison_includes_document_table_and_point_origin` asserts that `_same_native_map` rejects a changed document table or point origin and that the assembly-to-project map names exactly the three campaign projects. The marker amendment adds, fixture as data strings: in `test_csharp_coverage.py`, `test_prepare_and_runner_commands_are_exact_and_unfiltered` asserts `--showGenerated` absent from the prepare command (red at HEAD, where it asserts presence); in `test_csharp_join.py`, the attack table of `test_token_name_hash_alias_and_native_field_mismatches_fail_closed` gains the row `('vc="1" uspid="1"', 'vc="-2" uspid="1"', "sequence value")`, a retained pin that is green at HEAD because the negative check already exists; `csharp_join.py` is unchanged.

**Exact ownership delta.** The owned-path manifest remains 101 unique repository paths: no path is added or removed and no phase changes. Fourteen production actions change: `.config/dotnet-tools.json`; `dydo/guides/testing-strategy.md`; `dydo/reference/coverage-tools.md`; `DynaDocs.Tests/coverage/coverage.runsettings`; `csharp_coverage.py`; `csharp_join.py`; `metrics/AssemblyMetrics.cs`; `metrics/SourceMember.cs`; `metrics/SourceMetrics.cs`; `run_tests.py`; `tests/test_csharp_coverage.py`; `tests/test_csharp_join.py`; `tests/test_csharp_metrics.py`; and `DynaDocs.Tests/DynaDocs.Tests.csproj`. The 2026-09-09 document-classification amendment, made after campaign `native-20260908-01` disproved the root-relative document rule (`PDB source outside inventory root: ...\microsoft.net.test.sdk\18.0.1\build\net8.0\Microsoft.NET.Test.Sdk.Program.cs`), changes eleven action strings only: `metrics/AssemblyMetrics.cs`; `metrics/AssemblyFacts.cs`; `metrics/SourcePoint.cs`; `metrics/Program.cs`; `metrics/ProjectMetrics.cs`; `csharp_coverage.py`; `csharp_join.py`; `dydo/reference/coverage-tools.md`; `tests/test_csharp_metrics.py`; `tests/test_csharp_join.py`; and `tests/test_csharp_coverage.py`. `gate_collect.py`, `metrics/CompiledMethod.cs` and the feature file are unchanged, and the campaign command, identity/alias rules, branch identity, policy CC, thresholds and native evidence stay byte-for-byte. Eighteen actions therefore differ from the de702c5b manifest; every other one of the 83 actions and all exclusions/serial transfers remain byte-for-byte unchanged. The specification and portable packet paths are evidence for this specify hop, not production ownership. DYD-120 and every retained worktree remain excluded. The 2026-09-09 marker-and-cap amendment (`showgenerated-marker-evidence.md`) changes five action strings only: `csharp_coverage.py`; `tests/test_csharp_coverage.py`; `tests/test_csharp_join.py`; `windows_job.py`; and `tests/test_windows_job.py`; `csharp_join.py`, `run_tests.py`, `dydo/guides/testing-strategy.md` and `dydo/reference/coverage-tools.md` (neither names the flag at HEAD) and the feature file are unchanged, and no threshold, filter, assembly selection or campaign structure moves. Twenty actions therefore differ from the first committed manifest at `633a78e1` (the baseline the previous count measured; `de702c5b` predates the manifest); every other one of the 81 actions and all exclusions/serial transfers remain byte-for-byte unchanged. The original-token amendment made from `native-g-20260909-hop2-*` changes no owned path, phase, threshold, filter, assembly selection, policy, campaign command or action string: it corrects the already-owned `csharp_coverage.py`, `csharp_join.py`, `tests/test_csharp_coverage.py` and `tests/test_csharp_join.py` route so template tokens bind to originals despite instrumented renumbering, and makes exact restoration proof explicit. DR048, architecture, G/M requirements and the three reviewed amendments remain unchanged.

The existing 48 Python, 67 C# and 19 Node partial findings remain unreviewed measurements, not confirmed product bugs or remediation authority. M103 remains separate and unavailable. After this amendment, a fresh independent SPEC PASS is required before bounded implementation resumes; implementation then completes G-measure and classification before any separately owned remediation. Every existing G-final, hardening, CODE, merge and evidence gate remains required.

Pinned source metrics are Sonar C#10.33.0.1635/Roslyn5.9.0/Mono.Cecil0.11.6; Python complexipy8.0.0, radon6.0.1, ruff0.16.6, vulture2.16, coverage7.16.0; JS ESLint10.10.0, sonarjs4.2.0, c8 12.0.0, jscpd5.1.2, dependency-cruiser18.2.0, knip6.34.0. Retain exact transitive locks; remove mutation-only packages from the G-owned dependency manifests (DYD-103 owns its separate environment). Verify actual development Node22.13/.NET10.0.300/CPython3.12.14 and dependency closure before trusting facts. These are retained compatibility pins, not newly executed proofs. Tools remain local development dependencies, not shipped runtime packages.

### Isolation, distribution and ownership

All .NET tests use the bundled Python `DynaDocs.Tests/coverage/run_tests.py` under ordinary pipes; preserve DYD-122's PTY/stdin finding. Each coverage campaign owns one isolated snapshot and unique artifacts. Adopt retained Windows Job containment only at the single campaign process boundary, preserving its explicit Windows/CPython support and descendant/handle cleanup proof. Its existing240-second cap is insufficient for full suites: an explicit finite campaign limit1800 seconds and teardown10 seconds must be tested, with no deadline-renewal loop. That bound is validated at the owner boundary by `windows_job.validate(value, execution_seconds_maximum=EXECUTION_SECONDS_MAXIMUM)`, the one module constant `EXECUTION_SECONDS_MAXIMUM = 1800` being its default and the `teardown_seconds` bound 10 unchanged; `run(value, environment=None, execution_seconds_maximum=EXECUTION_SECONDS_MAXIMUM)` passes the keyword to `validate` verbatim and does nothing else with it; `request()`, the request schema, `config_sha256`, the result schema and `main()` are unchanged, so a stdin request is always bound by the default (the maximum is an in-process caller argument, never a request field) and `run_tests.py` keeps `execution_seconds=1800` under the default. An explicit maximum is the finite ceiling of one caller, never a global raise: the DYD-103 mutation campaigns pass 14400 (admiral DECIDED 2026-09-09). `test_explicit_execution_seconds_maximum_is_validated_at_the_boundary` in `test_windows_job.py` proves it in one case: a request with `execution_seconds` 14400 for a subject that exits at once completes under `execution_seconds_maximum=14400` with `complete` true and `execution_seconds` 14400 in its result; 14401 under that maximum and 14400 under the default are `not_started` with no output directory; the existing invalid-control case keeps rejecting 1801 under the default. Unsupported host setup returns2. Do not add a broker, worktree scheduler or arbitrary process adoption. Never kill foreign processes or touch retained worktrees. Own snapshot cleanup must verify exact path and registration; save raw evidence before removal. Existing outer test invocations and gate controllers remain measurable through ordinary targeted fixture tests, not recursion into G.

The canonical facade is `DynaDocs.Tests/coverage/gap_check.py`. `dydo/reference/gap-check.example.py` is its derived raw-byte distribution copy; `sync_testing_example.py` performs that single exact copy (no parsing/compiler framework), and `--check` verifies it without writing. Inventory grants derived status only if the named source exists, bytes match and the actual producer is present/tested; divergence is2, not a clone waiver. Adjacent JSON files remain distinct authored project/adoption inputs. Existing parity tests remain, expanded to exercise the copy/check mechanism. DYD-91 receives this arrangement for final portable adoption; its example JSON is not rewritten to point at private DynaDocs producers.

Exact proposed production paths are enumerated in committed `dydo/agents/workspace/dyd96-portable-wip/owned-paths.json`; it remains an ownership proposal until the captain posts this correction and independent SPEC passes. Retained inputs and their per-file SHA/disposition are in committed `dydo/agents/workspace/dyd96-portable-wip/retained-disposition.json`. Preserve every retained branch/dirty byte/original report. No broad harvesting, reset or cherry-pick of incompatible driver history. Each adopted component appears in a new unsquashed implementation hop with source-to-destination hashes and adaptations named. Active111 compiler/config/template paths and118 root/npm release metadata are excluded. G docs update only actual testing guides/reference/glossary; shipped coding-standards template reflection is a later exact owner transfer, not an implicit edit during111.

### Required gates and acceptance stages

Set process-local PYTHON to `C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe` for bridges. Run commands under ordinary pipes, preserve native exits/raw stdout/stderr and exact candidate identity.

```powershell
$ConsolidationPython = 'C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$env:PYTHON = $ConsolidationPython
& $ConsolidationPython -m unittest discover -s DynaDocs.Tests/coverage/tests -p 'test_*.py'
node DynaDocs.Tests/coverage/node_tests.cjs
& $ConsolidationPython DynaDocs.Tests/coverage/run_tests.py
dotnet build DynaDocs.sln -c Release -warnaserror
dotnet bin/Release/net10.0/dydo.dll check
& $ConsolidationPython DynaDocs.Tests/coverage/sync_testing_example.py --check
& $ConsolidationPython DynaDocs.Tests/coverage/gap_check.py capabilities
& $ConsolidationPython DynaDocs.Tests/coverage/gap_check.py gate static
& $ConsolidationPython DynaDocs.Tests/coverage/gap_check.py gate coverage
& $ConsolidationPython DynaDocs.Tests/coverage/gap_check.py --force-run
```

Before native collection, prepare the locked local assurance environment: Python at dydo/_system/.local/static-gates/python/Scripts/python.exe, Node tools from DynaDocs.Tests/coverage/package-lock.json installed under that coverage directory, and AltCover 9.0.102 restored from .config/dotnet-tools.json. These explicit relative project paths retain the existing Windows-oriented setup; none point into another retained checkout. Missing dependency roots are not copied from the retained environment by assumption. `gate_adapter.py` uses an explicit configured local interpreter path relative to the repository for dependency-bearing measurement (and retains the caller Python for the portable facade), not a private absolute path or automatic install. Missing tooling yields2 with the exact restore command. The first unittest command above is additionally run with dydo/_system/.local/static-gates/python/Scripts/python.exe after provisioning; tests needing pinned dependencies launch that exact interpreter from their ordinary process seams rather than assuming those packages are in the bundled interpreter. Unit tests must not skip silently if it is absent; the bundled Python launches the .NET adapter. The final report names both interpreter identities. Dependency installation/provisioning is ordinary implementation setup, never performed merely by `capabilities` or `--help`.

Policy fixtures must prove threshold boundaries79.99/80,59.99/60,20/>20,7/8 and constructor exception; fully covered flat high-CC code must not fail solely for CC. All supported warnings/dead-code/ternary/clone/cycle gates get real failing and corrected fixtures. Native exact joins cover overloads/generics/ref/out, same-name namespaces, async/iterator, lambdas/local functions, constructor initializers, accessors/top-level code, Python repeated/nested/same-line callables and JS callbacks/entry points. Missing/stale/malformed/partial/duplicate/out-of-root source/report/tool facts, never-hit modules, suppressed coverage/analyzers, source changes during collection, independent continuation and cancellation/cleanup are explicit fixture cases. Coverage/adapters must be exercised through native processes and real reports, not mocks around every producer. Skipped/unavailable fixture execution is incomplete, not a green suite.

**G-measure checkpoint:** all mechanism fixture gates pass; all five mechanisms produce complete actual measured facts, with every valid repository violation inventoried and an honest nonzero where present. No outside-owned production remediation. Return each failure's path/member/gate/value/threshold/raw proof and proposed assignment to captain/admiral. DYD-105's known entry gap is explicitly dependency-incomplete until its reviewed merge. Other newly exposed technical gaps need a bounded correction, not a false complete inventory. This checkpoint does not mean CODE-ready, G-final or Done.

**G-final:** after exact assigned remediation and integrated source/105, full isolated tests/build/docs pass and the exact candidate's `--force-run` returns0 with zero valid gaps/violations, complete source accounting and tested association/copy semantics. Separate M remains DYD-103 and is not replaced by hardener mutants. Distinct Hardener must attack actual production seams with meaningful mutant/fault probes, preserve all originals and obtain fresh independent CODE review before the captain offers PR. DYD-130 then supplies current-target combined gates and independent MERGE review. No master landing/tag/registry/local CLI installation here.

## Plan

**Approach:** preserve the accepted facade; add one private gate adapter over retained inventory/static mechanisms and ordinary full-suite coverage, replace the selected-test witness graph with exact file associations, and measure before assigning remediation. No parallel lanes: inventory/role/coverage/adapter seams transfer sequentially on this Issue.

**Patterns to copy:** `gap_check.py:224` owns subprocess/result behavior; `run_tests.py:125` owns attributed worktree cleanup and `:140` preserves interruption during ownership publication; retained `inventory.py:9` canonical identity; `gate_run.py` command evidence/independent results; `gate_collect.py` native static collectors; `AssemblyMetrics.cs` supplies exact MVID/MethodDef/PDB identities and `csharp_join.py:230` supplies emitted/source ownership that is retained while its Coverlet lookup is replaced; `SourceMetrics.cs` supplies the source-callable inventory and syntax-node traversal boundary for the calibrated policy CC; `python_runtime.py:39` AST/code-object identities; `js_metrics.cjs:12` UTF-16 source methods; `gate_clones.py` AND-threshold accounting; `windows_job.py:438` one owned native campaign. Native AltCover patterns and hashes are pinned by `native-altcover-evidence/SHA256SUMS`. Copy only after tests demonstrate the adapted boundary; the disposition file records rejected complexity.

**Files:** exact101-path action/transfer table is attached as the owned-path manifest. The retained producer records are split into their named files to obey the existing one-type-per-file standard; this preserves their semantics and does not introduce a new compiler framework. No catch-all production ownership. Gate fixture source is authored as data strings in the named tests, avoiding an unenumerated tree of compileable fixtures. Existing CLI E2E tests are read-only: prebuilding/instrumenting their current root DLL seam avoids118's owned test/version file. If native probes disprove that seam, stop for ownership/route amendment before editing it. Marker-and-cap files, one edit each: `csharp_coverage.py` deletes the `"--showGenerated",` element from the `prepare` list in `altcover_commands`; `tests/test_csharp_coverage.py` turns the `--showGenerated` `assertIn` into `assertNotIn`; `tests/test_csharp_join.py` adds the `vc="-2"` attack row named in the Spec; `windows_job.py` adds `EXECUTION_SECONDS_MAXIMUM = 1800` above `request`, gives `validate` the parameter `execution_seconds_maximum=EXECUTION_SECONDS_MAXIMUM` used in place of the literal 1800 in the deadline tuple, and gives `run` the same keyword passed on to `validate`; `tests/test_windows_job.py` adds the one boundary test named in the Spec. `csharp_join.py`, `run_tests.py`, both testing docs and the feature file: no edit. Grammar-rejection file, one edit: `metrics/Program.cs` replaces the `["--assembly", var assembly, "--root", var root]` arm with `["--assembly", var assembly, "--root", var root, "--project", var project] => AssemblyMetrics.Collect(assembly, root, project)` and sets the `_` arm's `ArgumentException` message to the pinned string of the Document classification paragraph; `tests/test_csharp_metrics.py` carries the three-leg case named in the Spec.

**Steps:**

1. Captain records the approved facade-exit ruling, posts this complete Spec/Plan and exact ownership, records the specify hop and commissions independent SPEC review. No implementation begins before PASS.
2. Implementer writes real failing boundary fixtures for the approved exit amendment, inventory/file association and copy provenance. Adopt inventory/positions/source tokens, exact locks and evidence/policy primitives, remove obsolete generated-workflow/tier/receipt/mutation coupling, and prove closed schema/fingerprints against untracked/deleted/renamed sources. Deliver the exact reviewed inventory interface to103 at this point, without declaring its production ready until the artifact shape is implemented and independently checked.
3. Adopt native static producers and their focused tests. Prove effective build/analyzer rules and deliberate native diagnostic failures before trusting repository measurements. Record complete tool versions and exact source joins. Do not fix product findings while building measurement.
4. Implement isolated ordinary full-suite coverage in order C#, Python, Node. For C#, replace Coverlet with the exact pinned AltCover eager section, prove original/instrumented/post identities and source-level Roslyn CC before consuming physical token rows, and preserve both failed and successful raw attempts. At each stack, first prove failing/passing never-called/child-only/same-name/entry fixtures and actual report identity. Retain only the necessary exact callable counter, not retired sessions. Test the campaign deadline/descendant cleanup. Any native limitation which changes this route returns to Specifier rather than growing a new framework. Before the first eager campaign of this hop, land the marker and cap amendment red before green: flip the `--showGenerated` assertion and delete the flag, add the `-2` pin, then add the boundary test, the constant and the threaded keyword.
5. Wire static/coverage commands and required reports into current JSON; preserve independent operations, public acceptance and derived raw-byte copy. Expand Node's real test suite discovery. Run all mechanism/functional gates and collect the first complete G-measure report. Return bounded remediation ownership requests and any concrete technical gap; do not call this final acceptance.
6. Captain/admiral assigns measured remediation under existing ownership rules. After reviewed source and105 integration, refresh this branch through explicit owned merge operations as required, rerun all exact gates, and complete Hardener/CODE/PR/130 through the normal chain. Final G=0 is required before96 Done;103 supplies final M separately.

**Edges:** valid peer findings remain visible after measurement gaps; unsupported hosts/tools and stale or conflicting native reports return2; coverage and association are independent; derived-copy mismatch is2; no generated-looking class or tiny entrypoint exclusion; no constructor-facade refactor to evade parameter policy. A child exit cannot be reinterpreted silently across the facade seam. Actual native facts prevail over assumptions about tool output; a disproved interface returns to specification. Historical legacy CRAP30 observations from111 remain leads, not HCRAP findings. A negative `vc` in any sequence or branch row is exit 2 whatever wrote it, the `-2` marker included; an explicit `execution_seconds_maximum` below the requested value, or a non-numeric one, rejects before launch with no output directory exactly as an over-limit request does; a stdin request cannot name its own maximum.

**Plan review: recommended and mandatory.** Material risks are the non-test exit amendment, ordinary full-suite process coverage, exact callable identity, self-measured tooling, analyzer configuration, dependency isolation and the103/105 interfaces. This pass has not executed native feasibility or claimed that these mechanisms are already complete. Lanes: none. Specify hop: nonempty feature-only commit for assurance-adoption.feature; no production or binding is written by the Specifier. All production/hardening hops remain required.
