---
area: general
type: changelog
date: 2026-05-04
---

# Task: fix-auto-resume-defects

Review the auto-resume defect bundle implementation per dydo/agents/Charlie/archive/20260501-204903/plan-fix-fix-auto-resume-defects.md (the original plan archive; Charlie's plan-fix-auto-resume-defects.md is in the archive dir after claim cycling). The bundle covers all 5 inquisition findings from project/inquisitions/auto-resume.md plus the new bad-session-ID fail-fast. Implementation landed across two commits: 9b27195 (the user-bundled wait+crash-resume fixes including all of my code; touches Services/WatchdogService.cs, AgentRegistry.cs, IAgentRegistry.cs, MacTerminalLauncher.cs, WindowsTerminalLauncher.cs, TerminalLauncher.cs, ProcessUtils.Ancestry.cs, WatchdogLogger.cs, Models/AgentState.cs and tests under DynaDocs.Tests/Services/) and a4726d8 (refactor on top to bring Services/WatchdogService.cs CRAP under threshold by extracting TryReadResumeContext + TryReadSession + IsBadSessionFailFast + per-field ParseResumeFields helpers). Findings addressed - HIGH#1/#0151 watchdog never registers anchors on Windows because FindAncestorProcess('claude') misses 'node' (new ProcessUtils.FindClaudeAncestor accepts 'node' on Windows; wired into WatchdogService.EnsureRunning anchor and AgentRegistry.ResolveClaimedPid); HIGH#2/#0152 resume-attempts cap exhaustible by single crash (new ResumeWarmupGate constant with 60s default, gated inside PollAndResumeForAgent so further launches suppressed during warmup); HIGH#3/#0153 resume-attempts not reset on same-session reclaims (new ResetResumeBookkeeping helper called in HandleExistingSession's same-session branch; clears ResumeAttempts/LastResumeLaunchedAt/PreResumePid); MED#4/#0154 Linux/Mac watchdog dies via anchor_gone when dispatchers exit (RegisterAnchor now called inside SetupAgentWorkspace on every claim); LOW#5/#0144 LaunchResume drops useTab and windowName (signatures threaded through TerminalLauncher facade and Windows/Mac launchers; useTab implied by windowId non-null); plus the new bad-session-ID fail-fast (PreResumePid recorded at launch; if ClaimedPid still equals it after gate elapses, SaturateResumeAttempts to cap and log no_refresh_after_warmup). State.md gains last-resume-launched-at and pre-resume-pid fields; cleared on claim and release. Tests added across ProcessUtilsTests, WatchdogServiceTests, AgentRegistryTests, TerminalLauncherTests covering all new branches plus 4 defensive tests for malformed state/session JSON. Full suite green at 4040 passed; gap_check 137/137 modules clean. Decision 022 doc edit is OUT OF SCOPE for this code-writer slice (dydo/project/decisions is read-only for code-writer); a docs-writer should be dispatched separately to clarify the spec wording around 'claim' inclusiveness, document the new state.md fields, and add the warmup-gate behaviour. Linux Finding 5 (resume always opens new window) is also out of scope - structural fix needs persisting terminal-binary + tab-mode in state.md and is its own design conversation. Reviewer flow: read the plan archive, read the diff between 756bedb (parent of all this work) and HEAD, run python DynaDocs.Tests/coverage/gap_check.py to confirm green, then approve or request changes via msg on this task subject.

## Progress

- [ ] (Not started)

## Files Changed

(None yet)

## Review Summary

Review the auto-resume defect bundle implementation per dydo/agents/Charlie/archive/20260501-204903/plan-fix-fix-auto-resume-defects.md (the original plan archive; Charlie's plan-fix-auto-resume-defects.md is in the archive dir after claim cycling). The bundle covers all 5 inquisition findings from project/inquisitions/auto-resume.md plus the new bad-session-ID fail-fast. Implementation landed across two commits: 9b27195 (the user-bundled wait+crash-resume fixes including all of my code; touches Services/WatchdogService.cs, AgentRegistry.cs, IAgentRegistry.cs, MacTerminalLauncher.cs, WindowsTerminalLauncher.cs, TerminalLauncher.cs, ProcessUtils.Ancestry.cs, WatchdogLogger.cs, Models/AgentState.cs and tests under DynaDocs.Tests/Services/) and a4726d8 (refactor on top to bring Services/WatchdogService.cs CRAP under threshold by extracting TryReadResumeContext + TryReadSession + IsBadSessionFailFast + per-field ParseResumeFields helpers). Findings addressed - HIGH#1/#0151 watchdog never registers anchors on Windows because FindAncestorProcess('claude') misses 'node' (new ProcessUtils.FindClaudeAncestor accepts 'node' on Windows; wired into WatchdogService.EnsureRunning anchor and AgentRegistry.ResolveClaimedPid); HIGH#2/#0152 resume-attempts cap exhaustible by single crash (new ResumeWarmupGate constant with 60s default, gated inside PollAndResumeForAgent so further launches suppressed during warmup); HIGH#3/#0153 resume-attempts not reset on same-session reclaims (new ResetResumeBookkeeping helper called in HandleExistingSession's same-session branch; clears ResumeAttempts/LastResumeLaunchedAt/PreResumePid); MED#4/#0154 Linux/Mac watchdog dies via anchor_gone when dispatchers exit (RegisterAnchor now called inside SetupAgentWorkspace on every claim); LOW#5/#0144 LaunchResume drops useTab and windowName (signatures threaded through TerminalLauncher facade and Windows/Mac launchers; useTab implied by windowId non-null); plus the new bad-session-ID fail-fast (PreResumePid recorded at launch; if ClaimedPid still equals it after gate elapses, SaturateResumeAttempts to cap and log no_refresh_after_warmup). State.md gains last-resume-launched-at and pre-resume-pid fields; cleared on claim and release. Tests added across ProcessUtilsTests, WatchdogServiceTests, AgentRegistryTests, TerminalLauncherTests covering all new branches plus 4 defensive tests for malformed state/session JSON. Full suite green at 4040 passed; gap_check 137/137 modules clean. Decision 022 doc edit is OUT OF SCOPE for this code-writer slice (dydo/project/decisions is read-only for code-writer); a docs-writer should be dispatched separately to clarify the spec wording around 'claim' inclusiveness, document the new state.md fields, and add the warmup-gate behaviour. Linux Finding 5 (resume always opens new window) is also out of scope - structural fix needs persisting terminal-binary + tab-mode in state.md and is its own design conversation. Reviewer flow: read the plan archive, read the diff between 756bedb (parent of all this work) and HEAD, run python DynaDocs.Tests/coverage/gap_check.py to confirm green, then approve or request changes via msg on this task subject.

## Code Review

- Reviewed by: Adele
- Date: 2026-05-02 10:58
- Result: PASSED
- Notes: PASS. Implementation cleanly matches plan archive (20260501-204903/plan-fix-auto-resume-defects.md) for all 5 inquisition findings + bad-session fail-fast. ProcessUtils.FindClaudeAncestor wired into both anchor and ClaimedPid paths (#0151). ResumeWarmupGate (60s) gates duplicate launches inside PollAndResumeForAgent (#0152). ResetResumeBookkeeping wipes attempts/lastAt/preResumePid in same-session reclaim and is symmetric with SetupAgentWorkspace and ReleaseAgent (#0153). RegisterAnchor in SetupAgentWorkspace is best-effort and try/catch-guarded as planned (#0154). LaunchResume signatures threaded through TerminalLauncher facade into Win/Mac with useTab implied by windowId!=null (#0144). Bad-session fail-fast saturates cap and emits structured no_refresh_after_warmup log; preResumePid! deref is safe because IsBadSessionFailFast guards the null. Refactor commit a4726d8 cleanly extracts ResumeContext + TryReadResumeContext + TryReadSession + IsBadSessionFailFast and per-field parsers; behaviour preserved. Lock discipline correct: TryReadResumeContext acquires/releases the per-agent .claim.lock before delegating to registry (which takes its own lock), matching the documented invariant that the launcher must not hold the agent's claim lock. ResetResumeBookkeeping correctly relies on UpdateAgentState being lock-free and runs inside ClaimAgent which already holds the lock. Test coverage: every planned test present (4 ProcessUtils, 3 AgentRegistry incl. anchor + reset + release-clears, 8 WatchdogService incl. gate/symmetry/bad-session/counter-test/window-id propagation/no-window-id symmetry, 2 WindowsTerminalLauncher) plus 4 defensive tests for null/malformed JSON and malformed timestamps. gap_check.py exit 0, 137/137 modules clean. Minor dydo check warnings/errors are pre-existing schema drift (inquisition type validation, broken-link errors in issues/_index.md from earlier resolved-issue moves, template-additions missing-title) - none introduced by this PR's source changes; the only net new dydo check items are 2 inquisition type errors on the new auto-resume.md/wait-rearm-flood-deadlock.md files which conform to the established repo pattern, and missing-summary-paragraph warnings on auto-generated issue stubs (also matching the existing pattern). Decision 022 doc edit correctly deferred to docs-writer per brief (project/decisions read-only for code-writer). Linux Finding 5 correctly out of scope. No security, correctness, or AI-slop concerns.

Awaiting human approval.

## Approval

- Approved: 2026-05-04 21:51
