---
area: project
type: context
---

# dydo 3.0 Linear-Native Work-Model Evidence

This record seals the integrated acceptance evidence for Project 2: Adopt Linear-native work model.
It is a dated proof surface, not a repository mirror of Linear workflow state or a synchronization
contract.

## Reviewed contract and integrated revision

- Governing plan: [`1cf75a219e4a7a30397174e0ab79f4aff1326547`](https://github.com/bodnarbalazs/dydo/blob/1cf75a219e4a7a30397174e0ab79f4aff1326547/dydo/project/plans/dydo-3-linear-native-work-model.md)
- Binding decision: [DR 044 — Linear-Canonical PM and the dydo Knowledge Boundary](../decisions/044-linear-canonical-pm-and-dydo-knowledge-boundary.md)
- Fully merged audit base: [`5cceda39657d9023c7c456b4f754e594f7cd0410`](https://github.com/bodnarbalazs/dydo/commit/5cceda39657d9023c7c456b4f754e594f7cd0410)
- Retrospective amendment: [DYD-21](https://linear.app/bodnar-balazs/issue/DYD-21/amend-project-2-matrix-and-lane-5-hub-ownership),
  reviewed head [`b03a06adf7c46ba820a8fe91141713ae00a73ed4`](https://github.com/bodnarbalazs/dydo/commit/b03a06adf7c46ba820a8fe91141713ae00a73ed4),
  [PR 5](https://github.com/bodnarbalazs/dydo/pull/5), and exact merge
  [`c4aa06c0d44174c0fb341908a2573a68fd31bd7e`](https://github.com/bodnarbalazs/dydo/commit/c4aa06c0d44174c0fb341908a2573a68fd31bd7e).
- Chronological generated-navigation amendment: [DYD-22](https://linear.app/bodnar-balazs/issue/DYD-22/authorize-linear-native-generated-navigation-hubs),
  reviewed head [`168cbf1a00915247d25dc95871b035be8d66cd9e`](https://github.com/bodnarbalazs/dydo/commit/168cbf1a00915247d25dc95871b035be8d66cd9e),
  [PR 6](https://github.com/bodnarbalazs/dydo/pull/6), and exact merge
  [`f9cb8a38176950fee17e29d14ffd90f326514daa`](https://github.com/bodnarbalazs/dydo/commit/f9cb8a38176950fee17e29d14ffd90f326514daa).
- Chronological token-aware-summary amendment: [DYD-23](https://linear.app/bodnar-balazs/issue/DYD-23/authorize-token-aware-hub-summary-extraction),
  reviewed head [`09df58d3920bc41c90556131c7a7ff10f5114f2d`](https://github.com/bodnarbalazs/dydo/commit/09df58d3920bc41c90556131c7a7ff10f5114f2d),
  [PR 7](https://github.com/bodnarbalazs/dydo/pull/7), and exact merge
  [`df32c75369b87f4693791e233fa234f769505dc9`](https://github.com/bodnarbalazs/dydo/commit/df32c75369b87f4693791e233fa234f769505dc9).
- Reviewed token-aware product fix: [DYD-24](https://linear.app/bodnar-balazs/issue/DYD-24/fix-hub-summary-extraction-for-dotted-and-markdown-tokens),
  reviewed head [`56e6529c48d828835a2f449ef98c1d18725f199f`](https://github.com/bodnarbalazs/dydo/commit/56e6529c48d828835a2f449ef98c1d18725f199f),
  [PR 8](https://github.com/bodnarbalazs/dydo/pull/8), and exact merge
  [`1032d5d14de3f16a67f49e571d825413d2efcf88`](https://github.com/bodnarbalazs/dydo/commit/1032d5d14de3f16a67f49e571d825413d2efcf88).
- Audit branch: `codex/DYD-16`, created directly from the exact merged audit base, then fast-forwarded
  through all exact amendment/product merges without losing any of the five authorized working paths.
  `git merge-base --is-ancestor 1032d5d14de3f16a67f49e571d825413d2efcf88 HEAD` exited 0.
- Integrated Project-2 diff: 184 changed paths, including 22 additions and 22 deletions, from the
  published plan commit through the merged audit base.
- Final Lane-5 implementation diff against `1032d5d1…`: exactly the two authored records and three
  command-produced hubs. Total Project-integration diff against `5cceda3965…`: exactly those five paths
  plus the cumulatively amended governing plan and the two reviewed DYD-24 product/test paths.

The governing plan subsequently received six narrow, reviewed amendments: the
[`DocScannerTests` ownership seam](https://github.com/bodnarbalazs/dydo/commit/0b36d22128f7d84640aed35d3b834a86571d5189),
the [four direct-test ownership seams](https://github.com/bodnarbalazs/dydo/commit/9f875725316fa06fe941a4ea7973fc149959e93a),
the [evidence-only retired-PM predicate](https://github.com/bodnarbalazs/dydo/commit/8d703ab93c1c87bfe0d01df562b8186b15b7b405),
the [retrospective 25-path/Lane-5 migrations-hub amendment](https://github.com/bodnarbalazs/dydo/commit/c4aa06c0d44174c0fb341908a2573a68fd31bd7e),
the [chronological generated-navigation amendment](https://github.com/bodnarbalazs/dydo/commit/f9cb8a38176950fee17e29d14ffd90f326514daa),
and the [chronological token-aware-summary amendment](https://github.com/bodnarbalazs/dydo/commit/df32c75369b87f4693791e233fa234f769505dc9).

## Linear reference audit

The official Linear connector resolved fixed Project
[`44eba9ff-0242-4179-b94b-932339b364fd`](https://linear.app/bodnar-balazs/project/dydo-30-adopt-linear-native-work-model-8145ca3f78ad).
Its team is exactly `caa6ccbf-4f9b-477e-826c-a51ed43b0687` (`Dydo`), and its returned URL exactly
matches the plan frontmatter. The Project resource labeled “Reviewed Linear-native work-model plan
(1cf75a21)” returned the exact published plan permalink above.

The following is a one-time acceptance read-back, not a maintained state table. Every returned Issue
belonged to the fixed Project and team. The four predecessor lanes were actually `Done`; DYD-16's final
transition and exact evidence-commit link remain on its canonical Linear Issue after push rather than
being copied back into its own commit.

| Issue | One-time reference proof | Immutable delivery evidence |
|---|---|---|
| [DYD-12](https://linear.app/bodnar-balazs/issue/DYD-12/define-linear-native-doctrine-and-the-frozen-v2-compatibility-boundary) | `Done`; attached governing plan permalink at `1cf75a…` | [`38c70b63fb984aa8c91d11606bcee994d5111801`](https://github.com/bodnarbalazs/dydo/commit/38c70b63fb984aa8c91d11606bcee994d5111801); independent PASS recorded on the Issue |
| [DYD-13](https://linear.app/bodnar-balazs/issue/DYD-13/retire-the-local-task-and-issue-runtime) | `Done`; attached governing plan permalink at `1cf75a…` | [`3e1f7ce51ce677a4c0b3184ea4e413a63a1d72e6`](https://github.com/bodnarbalazs/dydo/commit/3e1f7ce51ce677a4c0b3184ea4e413a63a1d72e6); independent PASS recorded on the Issue |
| [DYD-14](https://linear.app/bodnar-balazs/issue/DYD-14/rewrite-templates-skills-and-workflows-for-linear-issues) | `Done`; attached governing plan permalink at `1cf75a…` | [PR 3](https://github.com/bodnarbalazs/dydo/pull/3), merge [`54b8d79d441f3e3a2df7d97bbf7e0a5338d885a7`](https://github.com/bodnarbalazs/dydo/commit/54b8d79d441f3e3a2df7d97bbf7e0a5338d885a7); independent PASS recorded on the parent and integration Issue |
| [DYD-15](https://linear.app/bodnar-balazs/issue/DYD-15/rewrite-active-product-docs-and-integration-proof) | `Done`; attached governing plan permalink at `1cf75a…` | [PR 4](https://github.com/bodnarbalazs/dydo/pull/4), merge [`5cceda39657d9023c7c456b4f754e594f7cd0410`](https://github.com/bodnarbalazs/dydo/commit/5cceda39657d9023c7c456b4f754e594f7cd0410); independent PASS recorded on the Issue |
| [DYD-16](https://linear.app/bodnar-balazs/issue/DYD-16/integrate-audit-and-assimilate-the-linear-native-work-model) | description links the governing `1cf75a…` plan; completion read-back is retained in Linear | Dedicated evidence branch `codex/DYD-16`; exact commit and strict verdicts are posted to the Issue after push |

DYD-14's visible decomposition was also read back: [DYD-17](https://linear.app/bodnar-balazs/issue/DYD-17/rewrite-dyd-14-product-facing-templates),
[DYD-18](https://linear.app/bodnar-balazs/issue/DYD-18/rewrite-dyd-14-role-and-methodology-templates),
[DYD-19](https://linear.app/bodnar-balazs/issue/DYD-19/rewrite-dyd-14-review-resources-and-workflows),
and [DYD-20](https://linear.app/bodnar-balazs/issue/DYD-20/integrate-and-regenerate-dyd-14-artifacts)
all returned the same fixed Project/team IDs and actual `Done` state. DYD-17 through DYD-19 descriptions
link the DYD-14 parent contract and exact `3e1f7ce5…` base. DYD-20's description links only the parent
contract and says integration starts after its source lanes; its exact `3e1f7ce5…` base is instead in its
immutable start/completion comments. Their delivery commits are `56027596…`, `818f87a6…`, `f0e716c4…`,
and `df2c33a0…`, respectively.

[DYD-21](https://linear.app/bodnar-balazs/issue/DYD-21/amend-project-2-matrix-and-lane-5-hub-ownership)
was read back as `Done` in the same fixed Project and team, parented by DYD-16, related to DYD-20,
and still blocking DYD-16. Its PR-5 attachment, reviewed head `b03a06ad…`, and merge `c4aa06c0…`
match the immutable Git evidence above. DYD-16 remained `In Progress` during this read-back; its actual
completion transition is recorded only in Linear after the evidence commit is pushed.

[DYD-22](https://linear.app/bodnar-balazs/issue/DYD-22/authorize-linear-native-generated-navigation-hubs)
was likewise read back as `Done` in the fixed Project/team, parented by DYD-16, related to DYD-21,
and blocking DYD-16. Its PR-6 attachment, reviewed head `168cbf1a…`, and merge `f9cb8a38…` match the
immutable Git evidence above. This is a one-time acceptance snapshot; no live status is mirrored here.

[DYD-23](https://linear.app/bodnar-balazs/issue/DYD-23/authorize-token-aware-hub-summary-extraction)
and [DYD-24](https://linear.app/bodnar-balazs/issue/DYD-24/fix-hub-summary-extraction-for-dotted-and-markdown-tokens)
were read back as `Done` in the same fixed Project/team and parented by DYD-16. DYD-23's PR-7
attachment and DYD-24's PR-8 attachment match the immutable Git evidence above; the returned relation
graph has DYD-23 blocking DYD-24 and DYD-16, and DYD-24 blocked by DYD-23 and blocking DYD-16.
DYD-16 remained `In Progress` during this one-time read-back.

## Command and exit ledger

The final rerun used exact amended master `1032d5d14de3f16a67f49e571d825413d2efcf88`.
The source-built Release binary generated and checked the record-bearing working tree. One Release test
build was reused by the five focused commands through `-c Release --no-build --no-restore`. The isolated
full-suite and forced-coverage runners each intentionally created a separate temporary worktree and
default Debug build at the same amended commit. The exact source-built binary is named wherever PATH
ambiguity mattered.

| Command | Exit | Evidence |
|---|---:|---|
| `dotnet build DynaDocs.csproj -c Release` | 0 | Source-built product Release succeeded; 0 warnings, 0 errors. |
| `dotnet bin/Release/net10.0/dydo.dll fix` | 0 | Repaired working-tree run reported 16 changes. Retained only migrations, understand, and guides hubs; restored twelve tracked collateral hubs and removed the command-created tasks hub. |
| Disposable candidate-tree Release build and source-built `fix`, followed by SHA-256 comparison | 0 | All three retained hubs byte-identical: migrations `829A0A52…`, understand `127B8FD0…`, guides `B6C2B738…`; 3 compared, 3 identical, 0 mismatches; temp/archive removed. |
| Exact navigation/scope assertion | 0 | Migrations links 5 in order; three DYD-22 entries and four repaired dotted/Markdown summaries occur exactly once; malformed forms and incomplete inline-code spans 0; project-root SHA-256 unchanged at `95BC0F72…`; tasks hub absent; implementation `5/0 unexpected`; integration `8/0 unexpected`. |
| `dotnet build DynaDocs.Tests/DynaDocs.Tests.csproj -c Release` | 0 | Test Release build succeeded; 0 warnings, 0 errors. |
| `dotnet test DynaDocs.Tests/DynaDocs.Tests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~HubGeneratorTests"` | 0 | 23 passed, 0 failed, 0 skipped; includes every token-aware extraction regression pin. |
| `pwsh -NoProfile -File dydo/project/migrations/build-3.0-pm-manifest.ps1 -RepoRoot . -Verify` | 0 | 474 records; 0 missing, duplicates, invalid references, or unresolved candidates. |
| `dotnet test DynaDocs.Tests/DynaDocs.Tests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~LegacyPmManifestServiceTests\|FullyQualifiedName~LegacyPmRecordRuleTests\|FullyQualifiedName~SummaryRuleTests\|FullyQualifiedName~BrokenLinksRuleTests\|FullyQualifiedName~HubFilesRuleTests\|FullyQualifiedName~OrphanDocsRuleTests\|FullyQualifiedName~FolderMetaFilesRuleTests\|FullyQualifiedName~DocScannerTests"` | 0 | 124 passed, 0 failed, 0 skipped. |
| `dotnet test DynaDocs.Tests/DynaDocs.Tests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~CompleteCommandTests\|FullyQualifiedName~CommandSmokeTests\|FullyQualifiedName~CompletionsCommandTests\|FullyQualifiedName~FixHubHandlerTests\|FullyQualifiedName~GuardCommandTests\|FullyQualifiedName~HelpCommandTests\|FullyQualifiedName~CliEndToEndTests\|FullyQualifiedName~GuardIntegrationTests\|FullyQualifiedName~InitCommandTests\|FullyQualifiedName~InitCheckIntegrationTests\|FullyQualifiedName~FixCommandIntegrationTests\|FullyQualifiedName~CompletionProviderTests\|FullyQualifiedName~ConfigFactoryTests\|FullyQualifiedName~ConfigServiceTests\|FullyQualifiedName~ConfigurablePathsTests\|FullyQualifiedName~DocScannerTests\|FullyQualifiedName~FolderScaffolderTests\|FullyQualifiedName~HubGeneratorTests\|FullyQualifiedName~RuleSkipPathsTests\|FullyQualifiedName~OffLimitsRuleTests"` | 0 | 411 passed, 0 failed, 0 skipped; covers help, completion, runtime removal, scaffold behavior, and the added HubGenerator regressions. |
| `dotnet test DynaDocs.Tests/DynaDocs.Tests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~TemplateGeneratorTests\|FullyQualifiedName~TemplateUpdateTests\|FullyQualifiedName~TemplateCommandTests\|FullyQualifiedName~TemplateOverrideTests\|FullyQualifiedName~ProcessWorkflowTests\|FullyQualifiedName~WayfinderHarmonyTests\|FullyQualifiedName~CodexSyncArtifactsE2ETests"` | 0 | 141 passed, 0 failed, 0 skipped; covers template, skill, and workflow parity. |
| `dotnet test DynaDocs.Tests/DynaDocs.Tests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~CommandDocConsistencyTests\|FullyQualifiedName~ValidateCommandTests\|FullyQualifiedName~DocumentationTests\|FullyQualifiedName~InitCommandTests\|FullyQualifiedName~CodexSyncArtifactsE2ETests"` | 0 | 98 passed, 0 failed, 0 skipped; covers active docs and fresh initialization. |
| Exact help/completion/fresh-scaffold script below | 0 | 28 help lines; 15 completions; retired help entries 0; retired completions 0; retired scaffold directories 0; FutureFeatures present; temp removed. |
| `dotnet bin/Release/net10.0/dydo.dll template update --diff` | 0 | 0 updated; 24 already current. |
| `dotnet bin/Release/net10.0/dydo.dll sync` followed by `git diff --ignore-space-at-eol --exit-code -- .agents/skills .codex/agents` | 0 / 0 | Canonical source-built sync completed with zero semantic generated-content diff. Windows checkout line-ending noise was restored without retaining an edit. |
| `dotnet bin/Release/net10.0/dydo.dll check` | 0 | Final record-bearing tree: 0 errors; 14 frozen historical orphan warnings across 1,335 files; no migrations/current-plan orphan warning. |
| Exact retired-PM reproduction script below | 0 | `raw=5; allowed=5; unexpected=0`. |
| Exact historical-fixture reproduction script below | 0 | Live consumers 0; active content includes 0. |
| Exact no-Linear-implementation reproduction script below | 0 | Runtime mentions 6, all documentation prose; structural/addition/path hits 0. |
| Exact 25-path allow-set/provenance commands below | 0 | `allowed=25; unexpected=0`; every path has exactly the single `df2c33a0323b1b5daf41fc7e1f93d6a19161ee76` introducing commit. |
| Exact source/generated parity commands below | 0 | 17 skill blob pairs and two workflow pairs identical; four bounded stable test hunks; one template hunk with identical guide; four disposable-copy agent outputs reproduced with zero mismatch. |
| `py DynaDocs.Tests/coverage/run_tests.py` | 0 | Separate isolated Debug build at exact amended commit; 2,735 passed; 25 intentional live-Notion skips; 0 failed. |
| `py DynaDocs.Tests/coverage/gap_check.py --force-run` | 0 | Separate isolated coverage Debug build at exact amended commit; repeated 2,735/25/0 test result; 133/133 modules passed tier requirements. |
| `git diff --name-only HEAD` and `git diff --name-only 5cceda39657d9023c7c456b4f754e594f7cd0410` | 0 | Exact implementation diff: 5 paths. Exact Project-integration diff: 8 paths: cumulative plan, two DYD-24 paths, and five DYD-16 paths. |
| `git diff --check` | 0 | Final five-path implementation diff is whitespace-clean. |

## Exact generated-hub and scope assertions

The disposable parity run used the exact amended `HEAD`, copied in the two uncommitted authored records,
built the candidate's source binary, ran its repo-root `fix`, and compared exact bytes:

```powershell
$repo = 'C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$safe = 'safe.directory=C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$tempRoot = [System.IO.Path]::GetFullPath([System.IO.Path]::GetTempPath())
$temp = [System.IO.Path]::GetFullPath((Join-Path $tempRoot 'DynaDocs-DYD16-hub-parity-1032d5d1'))
$zip = [System.IO.Path]::GetFullPath((Join-Path $tempRoot 'DynaDocs-DYD16-hub-parity-1032d5d1.zip'))
if (-not $temp.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
    (Split-Path -Leaf $temp) -ne 'DynaDocs-DYD16-hub-parity-1032d5d1') {
    throw 'Unsafe parity directory'
}
if (-not $zip.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
    (Split-Path -Leaf $zip) -ne 'DynaDocs-DYD16-hub-parity-1032d5d1.zip') {
    throw 'Unsafe parity archive'
}
if (Test-Path -LiteralPath $temp) { throw "Parity directory exists: $temp" }
if (Test-Path -LiteralPath $zip) { throw "Parity archive exists: $zip" }
New-Item -ItemType Directory -Path $temp | Out-Null
try {
    & git -c $safe archive --format=zip -o $zip HEAD
    if ($LASTEXITCODE -ne 0) { throw "git archive exit $LASTEXITCODE" }
    Expand-Archive -LiteralPath $zip -DestinationPath $temp
    Copy-Item -LiteralPath "$repo/dydo/project/migrations/3.0-linear-work-model.md" -Destination "$temp/dydo/project/migrations/3.0-linear-work-model.md"
    Copy-Item -LiteralPath "$repo/dydo/project/migrations/3.0-linear-work-model-assimilation.md" -Destination "$temp/dydo/project/migrations/3.0-linear-work-model-assimilation.md"
    Push-Location $temp
    try {
        & dotnet build DynaDocs.csproj -c Release
        if ($LASTEXITCODE -ne 0) { throw "disposable Release build exit $LASTEXITCODE" }
        & dotnet bin/Release/net10.0/dydo.dll fix
        if ($LASTEXITCODE -ne 0) { throw "disposable fix exit $LASTEXITCODE" }
    } finally {
        Pop-Location
    }
    $hubPaths = @(
        'dydo/project/migrations/_index.md',
        'dydo/understand/_index.md',
        'dydo/guides/_index.md'
    )
    $mismatches = [Collections.Generic.List[string]]::new()
    foreach ($path in $hubPaths) {
        $retainedHash = (Get-FileHash -Algorithm SHA256 -LiteralPath (Join-Path $repo $path)).Hash
        $generatedHash = (Get-FileHash -Algorithm SHA256 -LiteralPath (Join-Path $temp $path)).Hash
        Write-Output "$path retained=$retainedHash disposable=$generatedHash"
        if ($retainedHash -ne $generatedHash) { $mismatches.Add($path) }
    }
    if ($mismatches.Count -ne 0) { throw "Hub byte mismatch: $($mismatches -join ', ')" }
    Write-Output "Hub byte parity: compared=3; identical=3; mismatches=0."
} finally {
    if (Test-Path -LiteralPath $temp) { Remove-Item -LiteralPath $temp -Recurse }
    if (Test-Path -LiteralPath $zip) { Remove-Item -LiteralPath $zip }
}
Write-Output "Parity cleanup: temp_removed=$(-not (Test-Path -LiteralPath $temp)); archive_removed=$(-not (Test-Path -LiteralPath $zip))."
```

The exact content, protected-root, and path arithmetic assertions were:

```powershell
$safe = 'safe.directory=C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$implementationExpected = @(
    'dydo/guides/_index.md',
    'dydo/project/migrations/3.0-linear-work-model-assimilation.md',
    'dydo/project/migrations/3.0-linear-work-model.md',
    'dydo/project/migrations/_index.md',
    'dydo/understand/_index.md'
)
$integrationExpected = @(
    'DynaDocs.Tests/Services/HubGeneratorTests.cs',
    'Services/HubGenerator.cs',
    'dydo/guides/_index.md',
    'dydo/project/migrations/3.0-linear-work-model-assimilation.md',
    'dydo/project/migrations/3.0-linear-work-model.md',
    'dydo/project/migrations/_index.md',
    'dydo/project/plans/dydo-3-linear-native-work-model.md',
    'dydo/understand/_index.md'
)
$implementation = @(& git -c $safe diff --name-only HEAD)
$integration = @(& git -c $safe diff --name-only 5cceda39657d9023c7c456b4f754e594f7cd0410)
$implMissing = @($implementationExpected | Where-Object { $_ -notin $implementation })
$implUnexpected = @($implementation | Where-Object { $_ -notin $implementationExpected })
$intMissing = @($integrationExpected | Where-Object { $_ -notin $integration })
$intUnexpected = @($integration | Where-Object { $_ -notin $integrationExpected })
if ($implementation.Count -ne 5 -or $integration.Count -ne 8 -or
    $implMissing.Count -ne 0 -or $implUnexpected.Count -ne 0 -or
    $intMissing.Count -ne 0 -or $intUnexpected.Count -ne 0) {
    throw 'Exact scope arithmetic failed'
}
$expectedMigrationLinks = @(
    './3.0-linear-bootstrap.md',
    './3.0-linear-work-model-assimilation.md',
    './3.0-linear-work-model.md',
    './3.0-notion-freeze.md',
    './3.0-pm-records.md'
)
$migrationLinks = @(Get-Content -LiteralPath 'dydo/project/migrations/_index.md' |
    Where-Object { $_ -match '^- \[' } |
    ForEach-Object { if ($_ -match '\]\((?<path>\./[^)]+)\)') { $Matches.path } })
if ($migrationLinks.Count -ne 5 -or
    (Compare-Object $expectedMigrationLinks $migrationLinks -SyncWindow 0)) {
    throw 'Migrations link order failed'
}
$understandLines = @(Get-Content -LiteralPath 'dydo/understand/_index.md')
$guideLines = @(Get-Content -LiteralPath 'dydo/guides/_index.md')
$expectedEntries = @(
    '- [Linear Issue Lifecycle](./task-lifecycle.md) - Actionable work lives in Linear Issues.',
    '- [Work Model](./work-model.md) - Linear owns dydo''s live work graph; Git owns durable knowledge and proof.',
    '- [Writing Good Briefs](./writing-good-briefs.md) - The self-containment bar for a Linear Issue, Project-plan lane, or prompt handed to a fresh agent.'
)
$entryCounts = @(
    @($understandLines | Where-Object { $_ -ceq $expectedEntries[0] }).Count,
    @($understandLines | Where-Object { $_ -ceq $expectedEntries[1] }).Count,
    @($guideLines | Where-Object { $_ -ceq $expectedEntries[2] }).Count
)
if (@($entryCounts | Where-Object { $_ -ne 1 }).Count -ne 0) {
    throw "Corrected navigation entry count failed: $($entryCounts -join ',')"
}
$expectedRepaired = @(
    '- [dydo 3.0 Linear Bootstrap](./3.0-linear-bootstrap.md) - Identity and verification evidence for provisioning the reviewed dydo 3.0 work graph in Linear.',
    '- [Architecture Overview](./architecture.md) - DynaDocs is a .NET 10 CLI that authors and validates durable project knowledge, compiles native agent methods, and enforces universal guard rules.',
    '- [Customizing Roles](./customizing-roles.md) - The mode template is the role: `dydo sync` discovers `mode-<name>.template.md` sources and compiles their methodology into native skills and, for...',
    '- [Migrating a Project from dydo 1.x to 2.x](./migrating-dydo-1x-to-2x.md) - A self-contained, ordered procedure for an AI coding agent to migrate an existing project''s dydo workspace from the 1.x generation to the current...'
)
$migrationLines = @(Get-Content -LiteralPath 'dydo/project/migrations/_index.md')
$repairedCounts = @(
    @($migrationLines | Where-Object { $_ -ceq $expectedRepaired[0] }).Count,
    @($understandLines | Where-Object { $_ -ceq $expectedRepaired[1] }).Count,
    @($guideLines | Where-Object { $_ -ceq $expectedRepaired[2] }).Count,
    @($guideLines | Where-Object { $_ -ceq $expectedRepaired[3] }).Count
)
if (@($repairedCounts | Where-Object { $_ -ne 1 }).Count -ne 0) {
    throw "Repaired dotted/Markdown summary count failed: $($repairedCounts -join ',')"
}
$incompleteCodeLines = @($guideLines | Where-Object {
    $_ -match '^- \[' -and ([regex]::Matches($_, [string][char]96).Count % 2) -ne 0
})
if ($incompleteCodeLines.Count -ne 0) {
    throw "Incomplete inline-code span in generated guide hub: $($incompleteCodeLines -join [Environment]::NewLine)"
}
$stale = @(& rg -n -i 'How tasks flow from creation through implementation, review, and completion|Slice\s*(→|->)\s*Sprint\s*(→|->)\s*Campaign\s*(→|->)\s*Release|a slice file.s implementation detail' dydo/understand/_index.md dydo/guides/_index.md)
if ($LASTEXITCODE -eq 0) { throw "Stale navigation remains: $($stale -join [Environment]::NewLine)" }
if ($LASTEXITCODE -ne 1) { throw "Stale navigation scan exit $LASTEXITCODE" }
$malformed = @(& rg -n 'dydo 3\.$|DynaDocs is a \.$|mode-<name>\.$|from the 1\.$' dydo/project/migrations/_index.md dydo/understand/_index.md dydo/guides/_index.md)
if ($LASTEXITCODE -eq 0) { throw "Malformed generated summary remains: $($malformed -join [Environment]::NewLine)" }
if ($LASTEXITCODE -ne 1) { throw "Malformed summary scan exit $LASTEXITCODE" }
$projectHash = (Get-FileHash -Algorithm SHA256 -LiteralPath 'dydo/project/_index.md').Hash
if ($projectHash -ne '95BC0F72B2F0A5DAEF89040EBFDC0A4285944128C08DDC124D3D52FA6BB01F7C') {
    throw 'Project root changed'
}
$tasks = Test-Path -LiteralPath 'dydo/project/tasks/_index.md'
if ($tasks) { throw 'Tasks hub exists' }
Write-Output "Scope/navigation: implementation=5/0 unexpected; integration=8/0 unexpected; migrations_links=5 ordered; corrected_entries=$($entryCounts -join ','); repaired_entries=$($repairedCounts -join ','); stale_entries=0; malformed_entries=0; incomplete_code_spans=0; project_root_unchanged=True; tasks_index_exists=$tasks."
```

## Exact help, completion, and fresh-scaffold assertions

```powershell
$repo = 'C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$dll = "$repo/bin/Release/net10.0/dydo.dll"
$help = @(& dotnet $dll --help)
if ($LASTEXITCODE -ne 0) { throw "help command exit $LASTEXITCODE" }
$helpText = $help -join [Environment]::NewLine
if ($helpText -match '(?m)^\s+(task|issue)\b') { throw 'retired help command exposed' }
$items = @(& dotnet $dll _complete 1 dydo)
if ($LASTEXITCODE -ne 0) { throw "completion command exit $LASTEXITCODE" }
if ($items -contains 'task' -or $items -contains 'issue') { throw 'retired completion exposed' }
$target = Join-Path ([System.IO.Path]::GetTempPath()) 'DynaDocs-DYD16-scaffold-c4aa06c0'
$tempRoot = [System.IO.Path]::GetFullPath([System.IO.Path]::GetTempPath())
$resolved = [System.IO.Path]::GetFullPath($target)
if (-not $resolved.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
    (Split-Path -Leaf $resolved) -ne 'DynaDocs-DYD16-scaffold-c4aa06c0') {
    throw 'Unsafe scaffold temp target'
}
if (Test-Path -LiteralPath $resolved) { throw "Scaffold temp already exists: $resolved" }
New-Item -ItemType Directory -Path $resolved | Out-Null
try {
    Push-Location $resolved
    try {
        & dotnet $dll init none
        if ($LASTEXITCODE -ne 0) { throw "init command exit $LASTEXITCODE" }
        $retired = @(
            'dydo/project/tasks',
            'dydo/project/issues',
            'dydo/project/sprints',
            'dydo/project/slices',
            'dydo/project/campaigns',
            'dydo/project/backlog'
        )
        $present = @($retired | Where-Object { Test-Path -LiteralPath $_ })
        $future = Test-Path -LiteralPath 'dydo/project/future-features'
        if ($present.Count -ne 0 -or -not $future) {
            throw "Fresh scaffold assertion failed: present=$($present -join ','); future=$future"
        }
    } finally {
        Pop-Location
    }
} finally {
    Remove-Item -LiteralPath $resolved -Recurse
}
if (Test-Path -LiteralPath $resolved) { throw 'Scaffold temp cleanup failed' }
Write-Output "Help/completion/scaffold: help_lines=$($help.Count); completions=$($items.Count); retired_help=0; retired_completions=0; retired_scaffold_dirs=0; future_features=True; temp_removed=True."
```

Exit 0 produced exactly `help_lines=28; completions=15; retired_help=0; retired_completions=0;
retired_scaffold_dirs=0; future_features=True; temp_removed=True`.

## Retrospective amendment reproduction

This exact executable script performed the closed-set, provenance, source/generated, bounded-hunk, and
disposable mechanical-regeneration proofs:

```powershell
$repo = 'C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$safe = 'safe.directory=C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$base = '1cf75a219e4a7a30397174e0ab79f4aff1326547'
$merged = '5cceda39657d9023c7c456b4f754e594f7cd0410'
$source = 'df2c33a0323b1b5daf41fc7e1f93d6a19161ee76'
$expected = @(
    '.claude/agents/inquisitor.md',
    '.claude/agents/reviewer.md',
    '.claude/skills/chief-of-staff/SKILL.md',
    '.claude/skills/co-thinker/SKILL.md',
    '.claude/skills/code-writer/SKILL.md',
    '.claude/skills/docs-writer/SKILL.md',
    '.claude/skills/grilling/SKILL.md',
    '.claude/skills/inquisitor/SKILL.md',
    '.claude/skills/orchestrator/SKILL.md',
    '.claude/skills/planner/SKILL.md',
    '.claude/skills/reviewer/SKILL.md',
    '.claude/skills/reviewer/resources/code.md',
    '.claude/skills/reviewer/resources/docs.md',
    '.claude/skills/reviewer/resources/merge-sprint.md',
    '.claude/skills/reviewer/resources/plan.md',
    '.claude/skills/reviewer/resources/tests.md',
    '.claude/skills/self-improvement/SKILL.md',
    '.claude/skills/test-writer/SKILL.md',
    '.claude/skills/wayfinder/SKILL.md',
    '.claude/workflows/inquisition.js',
    '.claude/workflows/run-sprint.js',
    '.codex/agents/inquisitor.toml',
    '.codex/agents/reviewer.toml',
    'DynaDocs.Tests/Commands/SyncCommandTests.cs',
    'Templates/how-to-use-docs.template.md'
)
$actual = @(& git -c $safe diff --name-only $base $merged -- .claude .codex DynaDocs.Tests/Commands/SyncCommandTests.cs Templates/how-to-use-docs.template.md)
if ($LASTEXITCODE -ne 0) { throw 'Allow-set diff failed' }
$missing = @($expected | Where-Object { $_ -notin $actual })
$unexpected = @($actual | Where-Object { $_ -notin $expected })
if ($expected.Count -ne 25 -or $actual.Count -ne 25 -or
    $missing.Count -ne 0 -or $unexpected.Count -ne 0) {
    throw "Allow-set mismatch: expected=$($expected.Count); actual=$($actual.Count); missing=$($missing.Count); unexpected=$($unexpected.Count)"
}
$wrongProvenance = [Collections.Generic.List[string]]::new()
foreach ($path in $expected) {
    $commits = @(& git -c $safe log --format=%H "$base..$merged" -- $path)
    if ($LASTEXITCODE -ne 0 -or $commits.Count -ne 1 -or $commits[0] -ne $source) {
        $wrongProvenance.Add($path)
    }
}
if ($wrongProvenance.Count -ne 0) {
    throw "Provenance mismatch: $($wrongProvenance -join ', ')"
}
$skillRelatives = @(
    'chief-of-staff/SKILL.md',
    'co-thinker/SKILL.md',
    'code-writer/SKILL.md',
    'docs-writer/SKILL.md',
    'grilling/SKILL.md',
    'inquisitor/SKILL.md',
    'orchestrator/SKILL.md',
    'planner/SKILL.md',
    'reviewer/SKILL.md',
    'reviewer/resources/code.md',
    'reviewer/resources/docs.md',
    'reviewer/resources/merge-sprint.md',
    'reviewer/resources/plan.md',
    'reviewer/resources/tests.md',
    'self-improvement/SKILL.md',
    'test-writer/SKILL.md',
    'wayfinder/SKILL.md'
)
$skillMismatch = [Collections.Generic.List[string]]::new()
foreach ($relative in $skillRelatives) {
    $claudeBlob = & git -c $safe rev-parse "HEAD:.claude/skills/$relative"
    $agentBlob = & git -c $safe rev-parse "HEAD:.agents/skills/$relative"
    if ($claudeBlob -ne $agentBlob) { $skillMismatch.Add($relative) }
}
$workflowPairs = @(
    @('Templates/workflow-inquisition.js', '.claude/workflows/inquisition.js'),
    @('Templates/workflow-run-sprint.js', '.claude/workflows/run-sprint.js')
)
$workflowMismatch = [Collections.Generic.List[string]]::new()
foreach ($pair in $workflowPairs) {
    $left = & git -c $safe rev-parse "HEAD:$($pair[0])"
    $right = & git -c $safe rev-parse "HEAD:$($pair[1])"
    if ($left -ne $right) { $workflowMismatch.Add("$($pair[0]) != $($pair[1])") }
}
$syncDiff = @(& git -c $safe diff --unified=5 "$source^" $source -- DynaDocs.Tests/Commands/SyncCommandTests.cs)
$syncHunks = @($syncDiff | Where-Object { $_ -match '^@@' })
$syncBlobAtSource = & git -c $safe rev-parse "$($source):DynaDocs.Tests/Commands/SyncCommandTests.cs"
$syncBlobAtHead = & git -c $safe rev-parse 'HEAD:DynaDocs.Tests/Commands/SyncCommandTests.cs'
$templateDiff = @(& git -c $safe diff --unified=5 "$source^" $source -- Templates/how-to-use-docs.template.md)
$templateHunks = @($templateDiff | Where-Object { $_ -match '^@@' })
$templateBlob = & git -c $safe rev-parse 'HEAD:Templates/how-to-use-docs.template.md'
$guideBlob = & git -c $safe rev-parse 'HEAD:dydo/guides/how-to-use-docs.md'
if ($skillRelatives.Count -ne 17 -or $skillMismatch.Count -ne 0 -or
    $workflowPairs.Count -ne 2 -or $workflowMismatch.Count -ne 0 -or
    $syncHunks.Count -ne 4 -or $syncBlobAtSource -ne $syncBlobAtHead -or
    $templateHunks.Count -ne 1 -or $templateBlob -ne $guideBlob) {
    throw 'Source/generated parity failure'
}
$tempRoot = [System.IO.Path]::GetFullPath([System.IO.Path]::GetTempPath())
$temp = [System.IO.Path]::GetFullPath((Join-Path $tempRoot 'DynaDocs-DYD16-agent-repro-c4aa06c0'))
$zip = [System.IO.Path]::GetFullPath((Join-Path $tempRoot 'DynaDocs-DYD16-agent-repro-c4aa06c0.zip'))
if (-not $temp.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
    (Split-Path -Leaf $temp) -ne 'DynaDocs-DYD16-agent-repro-c4aa06c0') {
    throw 'Unsafe reproduction directory'
}
if (-not $zip.StartsWith($tempRoot, [StringComparison]::OrdinalIgnoreCase) -or
    (Split-Path -Leaf $zip) -ne 'DynaDocs-DYD16-agent-repro-c4aa06c0.zip') {
    throw 'Unsafe reproduction archive'
}
if (Test-Path -LiteralPath $temp) { throw "Reproduction directory exists: $temp" }
if (Test-Path -LiteralPath $zip) { throw "Reproduction archive exists: $zip" }
New-Item -ItemType Directory -Path $temp | Out-Null
try {
    & git -c $safe archive --format=zip -o $zip HEAD
    if ($LASTEXITCODE -ne 0) { throw "git archive exit $LASTEXITCODE" }
    Expand-Archive -LiteralPath $zip -DestinationPath $temp
    Push-Location $temp
    try {
        & dotnet "$repo/bin/Release/net10.0/dydo.dll" template update
        if ($LASTEXITCODE -ne 0) { throw "template update exit $LASTEXITCODE" }
        & dotnet "$repo/bin/Release/net10.0/dydo.dll" sync
        if ($LASTEXITCODE -ne 0) { throw "sync exit $LASTEXITCODE" }
    } finally {
        Pop-Location
    }
    $agentPaths = @(
        '.claude/agents/inquisitor.md',
        '.claude/agents/reviewer.md',
        '.codex/agents/inquisitor.toml',
        '.codex/agents/reviewer.toml'
    )
    $agentMismatch = [Collections.Generic.List[string]]::new()
    foreach ($path in $agentPaths) {
        $generatedBlob = & git -c $safe hash-object --path=$path (Join-Path $temp $path)
        $expectedBlob = & git -c $safe rev-parse "HEAD:$path"
        if ($generatedBlob -ne $expectedBlob) { $agentMismatch.Add($path) }
    }
    if ($agentMismatch.Count -ne 0) {
        throw "Mechanical agent reproduction mismatch: $($agentMismatch -join ', ')"
    }
} finally {
    if (Test-Path -LiteralPath $temp) { Remove-Item -LiteralPath $temp -Recurse }
    if (Test-Path -LiteralPath $zip) { Remove-Item -LiteralPath $zip }
}
Write-Output "Retrospective matrix/parity: allowed=$($actual.Count); unexpected=$($unexpected.Count); provenance_mismatches=$($wrongProvenance.Count); skill_pairs=$($skillRelatives.Count); workflow_pairs=$($workflowPairs.Count); test_hunks=$($syncHunks.Count); template_hunks=$($templateHunks.Count); agent_outputs=$($agentPaths.Count); agent_mismatches=$($agentMismatch.Count); temp_removed=$(-not (Test-Path -LiteralPath $temp)); archive_removed=$(-not (Test-Path -LiteralPath $zip))."
```

Exit 0 produced exactly `allowed=25; unexpected=0; provenance_mismatches=0; skill_pairs=17;
workflow_pairs=2; test_hunks=4; template_hunks=1; agent_outputs=4; agent_mismatches=0;
temp_removed=True; archive_removed=True`.

## Exact scan reproduction

The retired-PM predicate executed unchanged from the amended governing plan, with one additional
`$rawHitCount` output used only for evidence:

```powershell
$retired = 'TaskCommand|TaskCreateHandler|TaskDoneHandler|TaskListHandler|TaskReviewHandler|IssueCommand|IssueCreateHandler|IssueListHandler|IssueResolveHandler|ReviewCommand|TaskFile|TaskStatus|IssueStatus|IssueSeverity|IssueFoundBy|GetTasksPath|GetIssuesPath|project/(tasks|issues|sprints|slices|campaigns|backlog)'
$roots = @('Program.cs','Commands','Models','Services','Utils','Rules','Serialization','Templates','dydo/_system/templates','.agents/skills','.claude','.codex','DynaDocs.Tests','README.md','dydo/index.md','dydo/reference','dydo/understand','dydo/guides','dydo.json') | Where-Object { Test-Path $_ }
$hits = @(& rg -n -i $retired $roots --glob '!Templates/sync-model.template.json' --glob '!DynaDocs.Tests/Sync/**' --glob '!DynaDocs.Tests/Rules/FrontmatterRuleTests.cs' --glob '!DynaDocs.Tests/Fixtures/**' --glob '!dydo/guides/migrating-dydo-1x-to-2x.md' --glob '!notion-sync.md' --glob '!notion-oss-survey.md')
if ($LASTEXITCODE -notin 0, 1) { throw "Retired-PM scan failed with exit $LASTEXITCODE" }
$rawHitCount = $hits.Count
$allowedRetiredMatches = @(
    @{ Path = 'DynaDocs.Tests/Services/LegacyPmManifestServiceTests.cs'; Text = '[InlineData("{\"path\":\"dydo/project/tasks/one.md\"}")]' }
    @{ Path = 'DynaDocs.Tests/Services/LegacyPmManifestServiceTests.cs'; Text = '[InlineData("{\"path\":\"dydo/project/tasks/one.md\",\"executionState\":1}")]' }
    @{ Path = 'DynaDocs.Tests/Services/LegacyPmManifestServiceTests.cs'; Text = '[InlineData("{\"path\":\"dydo/project/tasks/one.md\",\"executionState\":\"unknown\"}")]' }
    @{ Path = 'DynaDocs.Tests/Services/TemplateGeneratorTests.cs'; Text = 'Assert.DoesNotContain("project/tasks", content, StringComparison.OrdinalIgnoreCase);' }
    @{ Path = 'DynaDocs.Tests/Services/TemplateGeneratorTests.cs'; Text = 'Assert.DoesNotContain("project/issues", content, StringComparison.OrdinalIgnoreCase);' }
)
$allowedKeys = [Collections.Generic.HashSet[string]]::new([StringComparer]::Ordinal)
foreach ($allowed in $allowedRetiredMatches) {
    $key = "$($allowed.Path)`0$($allowed.Text)"
    if (-not $allowedKeys.Add($key)) { throw "Duplicate retired-PM allow tuple: $($allowed.Path): $($allowed.Text)" }
}
$allowedCounts = @{}
$unexpectedHits = [Collections.Generic.List[string]]::new()
foreach ($hit in $hits) {
    if ($hit -notmatch '^(?<path>.*?):(?<line>\d+):(?<text>.*)$') { throw "Unparseable retired-PM scan hit: $hit" }
    $path = $Matches.path.Replace('\', '/')
    $text = $Matches.text.Trim()
    $key = "$path`0$text"
    if ($allowedKeys.Contains($key)) { $allowedCounts[$key] = 1 + ($allowedCounts[$key] ?? 0) }
    else { $unexpectedHits.Add($hit) }
}
$invalidAllowed = @($allowedRetiredMatches | Where-Object {
    $key = "$($_.Path)`0$($_.Text)"
    ($allowedCounts[$key] ?? 0) -ne 1
})
if ($invalidAllowed.Count -gt 0) {
    throw "Retired-PM proof tuples missing or duplicated:`n$($invalidAllowed | ForEach-Object { "$($_.Path): $($_.Text)" } | Out-String)"
}
$allowedCount = @($allowedCounts.Values | Measure-Object -Sum).Sum
if ($allowedCount -ne 5 -or $unexpectedHits.Count -ne 0) {
    throw "Retired repo-PM consumer remains (allowed=$allowedCount; unexpected=$($unexpectedHits.Count)):`n$($unexpectedHits -join "`n")"
}
Write-Output "Retired-PM scan: raw=$rawHitCount; allowed=5; unexpected=0."
```

The historical fixture proof executed exactly as follows:

```powershell
$fixtureConsumers = @(& rg -n 'DynaDocs\.Tests[\\/]Fixtures' Program.cs Commands Models Services Utils Rules Serialization DynaDocs.Tests --glob '!DynaDocs.Tests/Fixtures/**' --glob '!DynaDocs.Tests/Sync/**')
if ($LASTEXITCODE -eq 0) { throw "Historical fixture corpus has a live consumer:`n$($fixtureConsumers -join "`n")" }
if ($LASTEXITCODE -ne 1) { throw "Historical-fixture consumer scan failed with exit $LASTEXITCODE" }
$fixtureContentIncludes = @(& rg -n '<Content[^>]*(Include|Update)="Fixtures[\\/]|<None[^>]*(Include|Update)="Fixtures[\\/]' DynaDocs.Tests/DynaDocs.Tests.csproj)
if ($LASTEXITCODE -eq 0) { throw "Historical fixture corpus is included as active test content:`n$($fixtureContentIncludes -join "`n")" }
if ($LASTEXITCODE -ne 1) { throw "Historical-fixture content-include scan failed with exit $LASTEXITCODE" }
Write-Output "Historical-fixture predicates: live-consumers=$($fixtureConsumers.Count); active-content-includes=$($fixtureContentIncludes.Count)."
```

The no-Linear implementation scan used these exact roots and structural patterns:

```powershell
$repo = 'C:/Users/User/.codex/worktrees/e2ec/DynaDocs'
$base = '1cf75a219e4a7a30397174e0ab79f4aff1326547'
$head = '1032d5d14de3f16a67f49e571d825413d2efcf88'
$runtimeRoots = @('Program.cs','Commands','Models','Services','Utils','Rules','Serialization','Sync','DynaDocs.csproj','dydo.json') | Where-Object { Test-Path $_ }
$linearMentions = @(& rg -n -i 'linear' $runtimeRoots)
if ($LASTEXITCODE -notin 0,1) { throw "Runtime Linear mention scan failed with exit $LASTEXITCODE" }
$nonTemplateMentions = @($linearMentions | Where-Object { $_ -notmatch '^Services[\\/]TemplateGenerator\.cs:' })
$pattern = 'using\s+Linear|namespace\s+Linear|class\s+\w*Linear\w*(Client|Cache|Mirror|Schema|Daemon|Poller|Webhook|Sync)|interface\s+\w*Linear\w*(Client|Cache|Mirror|Schema|Daemon|Poller|Webhook|Sync)|LinearClient|api\.linear\.app|linear\.app/api|LINEAR_(API_)?(KEY|TOKEN)|PackageReference[^>]*Linear|AddHostedService[^\r\n]*Linear|HttpClient[^\r\n]*Linear|Linear[^\r\n]{0,40}(Webhook|Poller|Daemon|Cache|Mirror|Schema|Sync)'
$structuralHits = @(& rg -n -i $pattern $runtimeRoots)
if ($LASTEXITCODE -notin 0,1) { throw "Structural Linear machinery scan failed with exit $LASTEXITCODE" }
$addedStructural = @(git -c safe.directory=$repo diff -U0 $base $head -- '*.cs' '*.csproj' '*.json' '*.js' | rg -n "^\+[^+].*($pattern)" -i)
if ($LASTEXITCODE -notin 0,1) { throw "Added structural Linear machinery scan failed with exit $LASTEXITCODE" }
$changed = @(git -c safe.directory=$repo diff --name-only $base $head)
$integrationNamedPaths = @($changed | Where-Object { $_ -match '(?i)(^|[/\\])[^/\\]*linear[^/\\]*(client|webhook|poller|daemon|mirror|cache|schema|sync)|(^|[/\\])[^/\\]*(client|webhook|poller|daemon|mirror|cache|schema|sync)[^/\\]*linear' })
if ($nonTemplateMentions.Count -ne 0 -or $structuralHits.Count -ne 0 -or $addedStructural.Count -ne 0 -or $integrationNamedPaths.Count -ne 0) { throw 'No-Linear implementation proof failed.' }
Write-Output "No-Linear implementation scan: runtime mentions=$($linearMentions.Count); outside-template=$($nonTemplateMentions.Count); structural=$($structuralHits.Count); added=$($addedStructural.Count); paths=$($integrationNamedPaths.Count)."
```

## Absence and boundary proofs

The exact retired-PM predicate scanned the surviving runtime, tests, templates, installed templates,
compiled skills, and active docs. Its five hits were exactly the three manifest-parser fixture strings
and two negative `Assert.DoesNotContain` assertions approved by the evidence-only amendment. Exact
path-plus-full-trimmed-source matching appeared once per tuple; every additional, missing, or duplicate
tuple fails the gate.

The Linear implementation scan found six literal `Linear` mentions in active runtime roots, all six in
`Services/TemplateGenerator.cs` documentation prose, and zero elsewhere. Structural scans found zero
client, token/key, SDK/API, schema, hosted daemon, poller, webhook, Markdown mirror, cache, or sync
implementation; the Project-2 diff added zero matching implementation lines and changed zero
integration-named paths. Project 5's existing Notion/sync compatibility subtree remains present and
untouched.

Lane 5 changed no production, test, template, historical-corpus, sync, or configuration path. Its only
generated paths are the source-built migrations, understand, and guides hubs authorized cumulatively
by the separately reviewed and merged single-plan DYD-21 and DYD-22 amendments. DYD-23 authorized the
separate two-path DYD-24 product fix without transferring either product path or any additional hub to
Lane 5. Project-3 disposition and Project-5 removal were not begun, and no repository↔Linear
synchronization mechanism was introduced.

## Related

- [Linear-work-model assimilation](./3.0-linear-work-model-assimilation.md) — Review verdicts, learned improvements, friction, and deferred follow-ups.
- [dydo 3.0 Linear-Native Work Model](../plans/dydo-3-linear-native-work-model.md) — Reviewed Project-2 contract.
- [dydo 3.0 Linear PM Migration](../plans/dydo-3-linear-migration.md) — Five-Project migration boundary.
