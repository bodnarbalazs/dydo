---
area: project
type: folder-meta
---

# Slices

Slice records — the atom of implementation for a sprint. Each file is one disjoint piece
of a sprint, small enough to review in one round (`ready → in-progress → done`). This folder
is the leaf of the Notion PM sync spine (Release → Campaign → Sprint → Slice → Issue).