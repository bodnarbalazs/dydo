---
area: general
name: swarm-0205
status: pending
created: 2026-07-12T15:41:23.5244731Z
assigned: Dexter
needs-human: false
---

# Task: swarm-0205

CODEX swarm fix ROUND 2 — issue 0205. Your APPROACH is right and your PROSE ESCAPES are VERIFIED CORRECT by a Claude reviewer: the structural skip in RuleSkipPaths (mirroring IsTemplateOrAddition) + backtick-escaping the body literals in 0205/0186/0188 (confined to body text, not titles/H1s/_index.md — confirmed genuinely removed from doc.Links via LinkExtractor.IsInsideInlineCode). KEEP all of that. The review FAILED on ONE thing: the `IsAutoGenerated` predicate is OVER-BROAD. Self-contained; report then RELEASE YOURSELF. Under the dydo guard + auto mode.

THE PROBLEM: `RuleSkipPaths.IsAutoGenerated` currently does `doc.Content.Contains(AutoGeneratedMarker, StringComparison.Ordinal)` — matches the banner ANYWHERE in the file. So any HAND-WRITTEN doc that merely QUOTES the auto-generated banner in prose (inside backticks) is silently exempted from ALL broken-link validation. This misfires on 4 real files in the tree TODAY: `dydo/project/issues/0205-*.md` (line 20 — the issue file itself!), `dydo/project/inquisitions/pre-tag-audit.md` (86), `dydo/project/inquisitions/dydo-check-drift.md` (108), `dydo/project/inquisitions/link-validator-fix-verification.md` (76). Because 0205 quotes the banner, the WHOLE 0205 file is skipped — which MASKS 0205's own title/H1 `[label](#section)` errors (the true residual is 4 errors across 0186+0205, not the 2 you reported — the bug hides its own sibling). This is a silent FALSE-NEGATIVE class (real broken links no longer checked in 3 inquisition reports + the issue file) — strictly worse than the visible false-positives you removed. Two compounding faults: (a) matches anywhere instead of line-anchored; (b) you REDECLARED a truncated copy of the marker literal instead of using the shared public constant.

FIX (build ON your round-1 diff — keep the prose escapes + the tests you have):
1. Use the SHARED public constant, do NOT redeclare the literal: `HubGenerator.AutoGenComment` (`Services/HubGenerator.cs:13`, public). Precedent: `Commands/.../FixHubHandler.cs:145` uses the full constant; `dydo/reference/configuration.md:320` documents it was made public precisely so other code can detect generated files WITHOUT redeclaring. Replace your truncated `AutoGeneratedMarker` literal with `HubGenerator.AutoGenComment`.
2. Anchor the match to a FULL LINE, so a doc that merely quotes the banner mid-line / in backticks is NOT skipped. HubGenerator emits the banner via `AppendLine` (it sits on its own line, near the top of generated hub/index files). Robust options (pick one): match `"\n" + HubGenerator.AutoGenComment + "\n"` (and also handle the file-start case, since a generated file may begin with it — e.g. also check a leading-line occurrence), OR split `doc.Content` into lines and return true only if some TRIMMED line EQUALS `HubGenerator.AutoGenComment`. A whole-line/line-equality check is cleanest and unambiguous. Verify against the 4 quoting files above: they must NO LONGER be skipped (their banner mention is mid-line inside backticks), while the genuine generated `_index.md` (banner on its own line) IS still skipped.
3. Add a THIRD test: a doc that QUOTES the marker inside backticks mid-line in prose AND contains a raw `[x](#missing)` link is STILL validated (reports the broken link) — the over-breadth guard the current tests miss. Keep your existing two tests (generated-file-with-marker-on-own-line → skipped; non-generated → errors).
4. Fix the `RuleSkipPaths` XML doc summary if it still says "Path-prefix predicates" — it now also hosts a content predicate.

VERIFY: `dotnet build DynaDocs.Tests/DynaDocs.Tests.csproj --no-restore` passes; run `dotnet test` filtered to the BrokenLinksRule tests. You MAY run the freshly-built validator `dotnet bin/Debug/net10.0/dydo.dll check dydo/project/issues` and confirm: the `_index.md` structural noise is gone, the 4 quoting files are validated again (0205's own title/H1 errors now VISIBLE — expected, they're the known residual), and no NEW spurious skips. Do NOT run the python coverage gate (0282) — the reviewer re-runs it. NOTE: the 0186/0205 title/H1 `#section` errors are a KNOWN residual (out of scope for escaping — the heading IS the title) that Adele will file as a follow-up; your job is NOT to eliminate those, just to stop the predicate from hiding them.

REPORT + RELEASE: `dydo msg --to Adele --subject swarm-0205-r2` with: how you line-anchored the match + that you used HubGenerator.AutoGenComment, the new over-breadth test, the validator output (confirming the 4 quoting files are re-validated), build/test results, ~time. THEN release yourself.

CONSTRAINTS: touch ONLY `Utils/RuleSkipPaths.cs` + `DynaDocs.Tests/Rules/BrokenLinksRuleTests.cs` (and `Rules/BrokenLinksRule.cs` ONLY if the call site needs a signature tweak — unlikely). Keep your round-1 prose escapes in 0205/0186/0188 intact. Do NOT touch other swarm agents' files (GuardCommand.cs, WorktreeCommand.cs, AgentRegistry.cs, Sync/, gap_check.py) or HubGenerator.

## Progress

- [ ] (Not started)

## Files Changed

(None yet)

## Review Summary

(Pending)