---
area: project
type: context
version: 1.3.0
date: 2026-03-09
status: in-progress
---

# v1.3.0 Release Plan

Stabilize v1.2 features and evolve the framework based on real-world usage. Everything ships together, developed in parallel.

---

## Slices

### A — Guard Hardening
Fix sub-agent hook evasion and pre-claim read restrictions. The guard is the backbone — if it's porous, everything built on top is theater.

- [x] Investigate how Agent tool sub-processes bypass PreToolUse hooks
- [x] Ensure unclaimed agents can only read entry-point files
- [x] Verify guard enforcement across all agent spawn paths

### B — Messaging / Wait / Dispatch Fixes
Make the features shipped in v1.2 actually work.

- [x] dispatch --wait background enforcement
- [x] inbox clear state sync
- [x] reply-pending guardrail
- [x] auto-close behavior
- [x] Clear stuck waiting states (Brian, Dexter)

### C — Workflow & Role Revamp
Align the documented workflow with how the system is actually used. See [decision 005](decisions/005-fresh-agent-over-wait-for-feedback.md), [decision 006](decisions/006-drop-interviewer-role.md), and plan at `agents/Iris/plan-v1.3-workflow-role-revamp.md`.

#### C1 — Workflow template + role docs
- [x] Simplify workflow template to 3 steps (claim → orient → work)
- [x] Keep `--inbox` as only flag, drop all others
- [x] Drop interviewer role (absorbed by co-thinker)
- [x] Rename tester → test-writer, update mode file
- [x] Update co-thinker mode file (primary entry point, scoping techniques)
- [x] Update all mode file templates for new workflow

#### C2 — New roles: orchestrator + inquisitor + judge
- [x] Orchestrator mode file (swarm coordination, dispatch + monitor)
- [x] Inquisitor mode file (adversarial QA, hypothesis-driven)
- [x] Judge mode file (dispute arbitration, panel-limit constraint)
- [x] Guard: orchestrator is graduation-only (requires prior co-thinker or planner)
- [x] Guard: judge has panel-limit constraint (max 3 per task)

#### C3 — `dispatch --worktree`
- [x] Add `--worktree` flag to `DispatchCommand.cs`
- [x] Git worktree lifecycle in `TerminalLauncher.cs` (create before, cleanup after)
- [x] Cross-platform shell commands (Linux, Windows, macOS)
- [x] Orphaned worktree cleanup (`git worktree prune`)

#### C4 — Issue tracker
- [x] `dydo issue create/list/resolve` commands
- [x] Issue file format in `dydo/project/issues/`
- [x] Integration with inquisitor pipeline (confirmed findings → issues)

#### C5 — Inquisition coverage
- [x] Rolling per-area report format at `dydo/project/inquisitions/`
- [x] `dydo inquisition coverage` command
- [ ] Time-based confidence tracking

### D — Custom Agent Modes
Let users define their own roles with RBAC.

- [x] User-defined role templates with permission sets (`.role.json` files in `_system/roles/`)
- [x] Integration with guard enforcement
- [ ] Documentation and examples

### E — Coverage System
Port three-tier coverage from LC, integrate via dynamic template embedding.

- [ ] Adapt gap_check.py for single-stack (.NET/Coverlet)
- [ ] Tier annotations in test files
- [ ] tier_registry.json for promotion tracking
- [ ] Deliver via template embedding (not base scaffold) — dogfood example

### F — Release Documentation
dydo eating its own dogfood.

- [x] This release plan
- [ ] Changelog cleanup
- [ ] Updated guides reflecting workflow changes
- [x] dydo/ folder as living example of the system

### G — Terminal Grouping + Agent Tree
Fix tab chaos in multi-agent workflows. See [decision 004](decisions/004-terminal-grouping-over-dashboard.md).

- [x] `wt -w 0` in TerminalLauncher.cs (same-window tab spawning)
- [x] macOS equivalent
- [x] `dydo agent tree` command (ASCII dispatch hierarchy)

### H — TerminalLauncher Platform Fixes
`--new-window` dispatch fails with `error 2147942402 (0x80070002)` — "The system cannot find the file specified." The launch command uses `powershell` which may not resolve on Windows 11 (needs `pwsh` or full path). Terminals still open despite the error.

- [x] Fix `powershell` resolution in launch command (platform-specific launchers)
- [x] Audit TerminalLauncher.cs for platform-specific assumptions
- [x] Test `--new-window` and `--tab` on Windows 11
- [x] Verify macOS launch commands

---

## Related

- [Decision 003 — Agent Messaging](decisions/003-agent-messaging.md)
- [Decision 004 — Terminal Grouping Over Dashboard](decisions/004-terminal-grouping-over-dashboard.md)
- [Decision 005 — Fresh Agent Over Wait-for-Feedback](decisions/005-fresh-agent-over-wait-for-feedback.md)
- [Decision 006 — Drop Interviewer Role](decisions/006-drop-interviewer-role.md)
